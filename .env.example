# docker compose 로 띄울 때 쓰는 값. 복사해서 `.env` 로 두고 채운다.
#
#   cp .env.example .env
#
# `.env` 는 저장소에 올라가지 않는다 (.gitignore).

# Postgres 비밀번호. 아무거나가 아니라 길게 만들어 쓴다:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
POSTGRES_PASSWORD=
