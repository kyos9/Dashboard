# Python
__pycache__/
*.pyc
.venv/
*.db
*.sqlite3

# Node / frontend
node_modules/
dist/
.vite/

# Editors / OS
.DS_Store
.vscode/
.idea/

# Env files
.env
.env.local

# 실행 중 쌓이는 로그 (진단 화면이 읽는 파일)
backend/logs/

# DB 백업 (복구용 — 저장소에 올릴 것이 아니다)
backend/backups/
/backups/

# 로그인 세션 서명 키 (서버마다 다르고, 새어나가면 남이 쪽지를 위조한다)
session.key
# 푸시 알림 서명 키 (같은 이유 — 새어나가면 남이 이 서버인 척 알림을 보낸다)
vapid.pem

# 커버리지 측정 산출물
.coverage
htmlcov/
