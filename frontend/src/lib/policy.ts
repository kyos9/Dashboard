/**
 * 개인정보처리방침·이용약관 (ROADMAP 4-4b).
 *
 * 링크는 `<Link>` 가 아니라 보통 `<a>` 로 건다 — 라우터 밖(헤더 아래 알림)에서도 쓰고, 방침은
 * 자주 여는 화면이 아니라 새로 열어도 잃을 게 없다. 구글 콘솔의 "개인정보처리방침 링크"에도
 * 이 주소를 적는다 (`https://<도메인>/privacy`, DEPLOY.md 12번).
 */
export const POLICY_PATH = '/privacy'

/** 방침을 고치면 이 날짜도 바꾼다 — 무엇이 언제부터인지가 방침의 일부다 */
export const POLICY_EFFECTIVE = '2026-09-26'
