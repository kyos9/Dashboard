# 진행 계획

지금은 한 사람이 자기 PC에서 쓰는 도구다. 목표는 다음과 같다.

> 사람들이 각자 구글 계정으로 들어와, 자기 포트폴리오를 등록하고, 기술적 시그널 +
> 매크로 + 재무 지표를 보고, **자기 AI 키로** 분석을 받고, 리밸런싱 가이드를 받는 도구.
> 폰에 설치해서 앱처럼 쓰고, 서버(오라클 무료 티어 우선)에서 돌린다.

이 문서는 거기까지 가는 순서와, 순서가 그래야 하는 이유를 적는다.

---

## 0. 지금까지 온 자리

어려운 쪽은 이미 지나왔다. Wilder ADX/DMI 직접 구현, 시세 제공자 3개 폴백, 시장별
거래일 캘린더, 환율 환산, 리밸런싱 주문 가이드 — 이건 정답이 없어서 판단을 계속
해야 하는 영역이라 오래 걸린다.

남은 일은 대부분 정답이 정해져 있다. 구글 로그인에는 표준 절차가 있고, Docker도
PWA도 명세대로 하면 된다. **항목 수는 많지만 헤매는 시간은 적다.**

정직하게 크다고 적어둘 것은 셋이다.

1. **사용자 분리** — 이 계획에서 유일한 구조 변경
2. **재무 지표 수집** — 새 데이터 소스는 시세 제공자를 만들 때만큼 든다
3. **규제 확인** — 기술이 아니고, 계획 자체를 멈출 수 있는 유일한 항목

---

## 1. 데이터 경계 — 이 계획에서 가장 가성비 좋은 규칙

코드를 한 줄도 안 쓰고 정하는 규칙인데, 이후 모든 작업의 비용을 좌우한다.

| 공용 (모든 사용자가 나눠 쓴다) | 사용자별 |
| --- | --- |
| 시세 (`price_daily`) | 보는 종목 목록 |
| 지표 (`indicator_daily`) | 보유수량 (`holding`) |
| 시그널 (`signal_daily`) | 매수 기록 (`buy_execution`) |
| 상장목록 (`krx_listing`) | 목표비중·밴드·리뷰 마감일 |
| 환율 **시세** (`fx_rate`) | 기준통화·직접 입력 환율 (`base_currency`, `fx_overrides`) |
| 매크로 지표, 재무 지표 *(예정)* | AI 키 *(→ 브라우저 보관, 서버에 두지 않는다)* |

**왜 중요한가.** 삼성전자 시세는 사용자가 100명이어도 하루 한 번만 받으면 된다.
지표도 시그널도 사용자와 무관하게 한 번 계산하면 전원에게 쓰인다. 경계를 제대로
그으면 **사용자가 늘어도 서버 부담과 외부 API 호출이 거의 늘지 않는다.** 반대로 대충
그어서 사용자마다 시세를 따로 받으면 비용도 차단 위험도 사람 수에 비례해 커진다.

**새 테이블을 만들 때마다 리비전을 남긴다** (`alembic revision --autogenerate`). 2단계에서
드리프트 검사를 걸어뒀으니 잊으면 테스트가 먼저 잡는다.

**지금 `stocks` 테이블은 두 역할을 겸하고 있다** — "내가 보는 종목"(사용자별)과
"이 티커의 시장·통화"(공용). 4단계에서 `Instrument`(공용) + `UserStock`(사용자별)로
쪼갠다.

### 코드와 대조해보니 — 4단계가 같이 옮겨야 하는 것 넷

분류 자체는 맞았다. `indicators.py`와 `signals.py`는 `Stock`도 설정도 읽지 않으므로,
지표·시그널이 공용이라는 건 코드로 확인된다.

문제는 **표가 적지 않은 것들**이었다. 넷 다 지금은 아무 증상이 없다가 사용자가 둘이
되는 순간 드러나고, 그때는 데이터가 이미 쌓인 뒤라 비싸다. 그래서 지금 적어둔다.

**1. `portfolio_settings`도 `stocks`처럼 두 역할을 겸한다 — 이쪽이 더 나쁘다.**
밴드 기본값·기준통화·직접 입력 환율은 사용자별인데, **환율 캐시(`usd_krw_rate`,
`usd_krw_updated_at`)는 공용**이다. 환율 시세는 모두에게 같기 때문이다. 이대로
사용자별로 쪼개면 100명이 같은 USD/KRW를 100번 따로 받아온다 — `fx.py`의 신선도 판정
(`STALE_AFTER`, 하루)도 사용자마다 따로 돌기 때문이다. **이 표가 막으려던 실패가
정확히 이것이다.** `UserSettings`(사용자별) + 환율 캐시(공용)로 나눈다.

**2. 공용 테이블 다섯 개가 전부 `stocks.ticker`를 외래키로 물고 있다** — `price_daily`,
`indicator_daily`, `signal_daily`, `buy_execution`, `holding`. `stocks`가 사용자별로
쪼개지면 **공용 데이터가 사용자별 테이블을 가리키게 된다.** 실제로 터지는 자리는
`DELETE /api/stocks/{ticker}/purge`다 — 지금은 딸린 행을 전부 지우므로, A가 삼성전자를
완전삭제하면 **B의 시세·지표 히스토리까지 사라진다.** 다섯 개의 외래키를 `Instrument`로
옮긴다.

**3. `Stock.active`가 공용 수집을 켜고 끈다** — 매일 갱신은 `active=True`인 종목만 받는데
(`pipeline.py`), `active`는 "*내* 화면에서 치웠다"는 사용자별 뜻이다. A가 치우면 B의
시세가 갱신을 멈춘다. 수집 대상은 **"누구 하나라도 보고 있는 종목"의 합집합**으로 정한다.

**4. `holding`의 기본키가 `ticker` 하나다** — 사용자별인데 키에 사용자가 없어서, 그대로면
한 티커에 보유수량이 딱 하나다. `(user_id, ticker)`로 간다. `buy_execution`도 중복 판정을
`(ticker, period_start, period_end)`로 하므로 같은 기간에 두 사용자 기록이 충돌한다.

### 개인화할 때 — 지표는 공용, 판단만 사용자별

언젠가 "내 ADX 기준은 25" 같은 걸 열어주게 된다. 그때 경계를 어떻게 다시 그을지
미리 정해둔다.

**지표는 객관적이고, 시그널은 판단이다.** 삼성전자의 ADX는 사용자가 100명이어도
같은 값이다. 사람마다 다른 건 *"ADX가 얼마 이상이면 추세로 볼 것인가"* 뿐이다.

그래서 `indicator_daily`는 **공용으로 두고**, 임계값만 사용자 설정으로 두고, 시그널은
**읽을 때 계산한다.** 저장량이 전혀 늘지 않는다. 이 패턴은 이미 절반 있다 — 대시보드의
무릎조건 네 칸은 `signal_daily`를 읽는 게 아니라 저장된 `indicator_daily`에서 그 자리에서
다시 계산한다.

| | 예 | 비용 |
| --- | --- | --- |
| **싼 개인화** | ADX 임계(20), 거래량비 임계(1.1), 이격도 기준, 비교 기간(5일) | 설정 몇 개. 저장량 그대로 |
| **비싼 개인화** | MA20 → MA15, ADX 14일 → 10일, 표준편차 20일 → 30일 | `indicator_daily`가 사람마다 달라진다 = **공용이 깨진다** |

아랫줄은 기간을 몇 가지로 정해두고 그것들을 미리 다 계산해 컬럼으로 갖고 있으면 된다.
자유 입력으로 열지 않는다.

> 덤: 이 방향은 3절 규제에서 **위험을 낮추는 쪽**이다. "우리가 정한 조건으로 판단해
> 드립니다"에서 "사용자가 정한 규칙으로 계산해 보여주는 도구"로 더 확실히 간다.

### 공용에서 개인으로 옮기는 값은 얼마인가

**"테이블이 크냐"가 아니라 "다시 만들 수 있느냐"가 정한다.**

| | 다시 만들 수 있나 | 옮기는 값 |
| --- | --- | --- |
| `price_daily` | 외부에서 다시 받는다 (오래 걸리고 차단 위험) | 중간 |
| `indicator_daily` · `signal_daily` | **저장된 시세에서 재계산** (네트워크 불필요) | 거의 없음 |
| `krx_listing` · 환율 캐시 | 다시 받는다 | 거의 없음 |
| `holding` · `buy_execution` | **못 만든다** — 사용자가 입력하고 확정한 것 | 진짜 마이그레이션 |

다행히 **공용 칸에 있는 건 전부 다시 만들 수 있는 것**이고, 못 만드는 것은 이미
사용자별 칸에 있다. 그래서 공용 쪽의 모양을 바꾸는 일은 대개 *테이블 고치고 재계산*으로
끝난다. (12종목 10년치 = 3만 행을 통째로 다시 계산하는 데 **1.2초**다.)

진짜 비싼 건 셋째 종류다 — **애초에 기록하지 않은 것.** 리밸런싱 이력, 재무 지표의
공시 시점이 여기 해당한다. 이건 전환이 아니라 영구 손실이라 코드로 못 고친다.

> **그래서 새 테이블을 만들 때 묻는 순서는 둘이다.**
> 1. 공용인가 사용자별인가
> 2. **다시 만들 수 있는가** — 아니라면, 지금 기록하지 않으면 영영 없다

> **앞으로 만드는 모든 테이블은 위 표의 어느 쪽인지 정하고 시작한다.**
> 한 테이블이 양쪽에 걸치면 그 자리에서 쪼갠다 — 미뤘을 때의 값이 위 1번이다.
> 이 규칙이 있어서 3단계(기능 확장)를 4단계(사용자 분리)보다 먼저 해도 안전하다 —
> 새로 붙일 매크로·재무는 둘 다 공용 쪽이고, AI 키는 서버에 두지 않기 때문이다.

---

## 2. 단계

단계 번호는 처음 정한 것을 그대로 둔다(커밋 메시지·버전 기록이 이 번호를 쓴다).
**진행 순서는 번호순이 아니다.**

| 순서 | | 단계 | 크기 | 핵심 |
| --- | --- | --- | --- | --- |
| 완료 | **0** | 기반 정리 ✅ | 작음 | 한 프로세스 · 로그 파일 · 진단 화면 |
| 완료 | **1** | 경계 확정 ✅ | 아주 작음 | 위 표 + 코드 대조 |
| 완료 | **2** | 배포 토대 ✅ | 중간 | Alembic · Postgres 대응 · Docker · 백업 |
| 코드는 완료 | **5** | 배포 ✅ | 중간 | **비밀번호 잠금** · 스케줄러 단일 실행 · HTTPS · `DEPLOY.md` — 남은 건 인스턴스를 만드는 일 |
| 설치·오프라인 완료 | **6** | PWA | 중간 | 설치 ✅ · 오프라인 ✅ · 모바일 ✅ — **남은 건 푸시 알림** |
| 이어서 | **3** | 기능 확장 | 큼 | 매크로(3a **기획 확정**) → 재무 → AI 분석 |
| 완료 | **P** | 포트폴리오 단순화 ✅ (v0.20.0) | 중간 | 매수 기록·적립 칸 걷어냄 · 평단가 · 현금 · 포트폴리오 리뷰 주기 · 리밸런싱 기록 — 아래 **2-2** |
| 완료 | **P+** | 속도 ✅ (v0.20.1) | 작음 | 대시보드 쿼리가 수십 년치를 다 줄 세우던 것(465→32ms) · 이미 받아둔 종목은 등록 때 다시 안 받음 · 캘린더 미리 준비 · 차트 코드는 열 때 받음 |
| **다음** | **4** | 사용자 분리 + 로그인 (**기획 확정**) | **가장 큼** | 4-0 ✅ · 4-1 표 분리 ✅ · 4-2 `user_id` 전파·격리 테스트 ✅ · 4-3 구글 로그인 ✅(실계정 확인 ✅) · 4-4a 손님 둘러보기 + 관리자 잠금 ✅ → **4-4b** 가입 승인 · 수집 합집합 · `purge` · 상한·쿨다운 · 방침 |
| 선택 | **7** | 앱스토어 | 중간 | Capacitor |

**남은 순서 (2-2 중간검토에서 다시 정함):** 4-4b → 6 푸시 알림 → 3b 재무 → 3c AI → 작은 일들 → (7).

### 왜 이 순서인가

처음에는 3 → 4 → 5 → 6 순이었다. **"5·6단계는 4단계를 요구한다"고 봤는데, 정확히는
*인증*을 요구하지 구글 로그인 + 사용자별 분리를 요구하는 게 아니다.** 혼자 쓰는 동안은
비밀번호 하나로 잠그면 5·6단계가 열린다.

그리고 **혼자 써도 배포에 값이 있다.** 가장 큰 것은 이것이다.

> 지금은 **내 PC를 켜둬야 매일 갱신이 돈다.** 며칠 꺼두면 그 기간 시세가 비고 매수 기간
> 판정도 같이 밀린다. 서버에 있으면 내 PC와 무관하게 돌고, **6단계 푸시 알림도 그래야
> 온다.** "시그널이 떴을 때 알림을 받고 연다"가 이 도구의 실제 사용 방식인데, 그건
> 서버 없이는 성립하지 않는다.

거기에 폰·회사PC 어디서나 열리는 것, 업데이트가 `update.bat` 대신 자동이 되는 것이 붙는다.

4단계를 미루는 데 드는 값은 **없다.** 나중에 할 때 기존 데이터는 전부 "사용자 1번 것"으로
넘기면 되는데, 그게 `Instrument`/`UserStock` 분리가 어차피 하는 일이다. 오히려 1절에서
찾은 넷이 4단계를 예상보다 크게 만들었으므로, 혼자 쓰는 동안(그 넷이 아무 증상도 없는
동안) 미뤄두는 편이 낫다.

3단계가 5·6보다 뒤로 간 이유는 다르다. 3단계는 **구조를 바꾸지 않고 끼워넣는 일**이라
아무 때나 할 수 있고, 배포 뒤에 하면 고칠 때마다 서버에 한 줄로 반영된다.

### 0단계 — 기반 정리 *(완료, v0.7.0)*

- ✅ 프런트를 빌드해 FastAPI가 직접 서빙 (`app/web.py`) → **프로세스 하나, 포트 하나**
  (`http://localhost:8000`). SPA 폴백은 `/api/*`를 삼키지 않고 404를 그대로 낸다.
- ✅ 로그를 파일로 (`backend/logs/app.log`, 5MB × 3 회전). 접속 기록(`uvicorn.access`)은
  일부러 뺐다 — 요청마다 한 줄씩 쌓여 정작 볼 경고를 회전으로 밀어낸다.
- ✅ 진단 화면 (경고·오류만 기본, 트레이스백 포함) + `GET /api/logs/download`
- ✅ `start-all.bat`이 콘솔 없이 뜨고(`scripts/hidden.vbs`), `stop.bat`으로 끈다.
  `update.bat`이 화면까지 다시 빌드한다 — 이걸 빼먹으면 코드를 받아도 옛 화면이 뜬다.

이후 모든 단계의 전제다. 그리고 서버에 올린 뒤 문제가 났을 때 들여다볼 유일한 수단이
로그다. **로그 보기는 반드시 인증 뒤에 둔다** — 종목·에러·내부 경로가 찍히고, 사용자가
여럿이 되면 남의 기록이 섞인다. *(5단계에서 비밀번호로, 4단계에서 계정으로)*

### 2단계 — 배포 토대 *(완료, v0.8.0)*

- ✅ **Alembic 도입** (`backend/migrations/`, `app/migrate.py`). 어려운 건 스키마를 바꾸는
  일이 아니라 **이미 쓰고 있는 DB를 받아들이는 일**이었다. 그 파일에는 테이블이 다 있지만
  Alembic의 기록은 없어서, 그대로 올리면 첫 리비전부터 실행하다 "이미 있다"에서 멈춘다.
  그래서 옛 방식으로 한 번 현행화한 뒤 `0001`을 **도장만 찍고**(stamp) 거기서 이어간다.
  이 경로는 딱 한 번만 일어나고, `db.py`의 옛 마이그레이션 코드는 그 자리에서 끝났다 —
  앞으로의 변경은 전부 리비전으로 간다.
- ✅ **모델·마이그레이션 드리프트 검사** (`test_alembic.py`). 모델에 컬럼을 추가하고
  리비전을 잊으면 **내 PC(테스트는 `create_all`)에서는 멀쩡하고 서버에서만 터진다.**
  가장 늦게, 가장 나쁜 자리에서 발견되는 종류라 자동으로 잡는다.
- ✅ `DATABASE_URL`을 통째로 받는다. 호스팅이 주는 `postgres://` 주소도 그대로 붙여넣을 수
  있다 (SQLAlchemy 2.0은 이 접두사를 거절하므로 내부에서 바꿔준다).
- ✅ Docker + compose. 저장소와 같은 폴더 구조를 이미지 안에서도 유지하고, 남아야 하는
  것은 전부 `/data` 볼륨으로 모았다. 포트는 `127.0.0.1`에만 연다.
- ✅ 백업. **켤 때(최근 12시간 내 없으면) · 매일 · 스키마 바꾸기 직전 · 수동(`backup.bat`)**
  네 갈래로 뜨고 7벌 보관한다. 압축하지 않는다 — 복구가 *파일을 제자리에 갖다 놓기*여야
  급할 때 실제로 복구된다. 돌고 있는 파일을 복사하지 않고 sqlite 백업 API를 쓴다.

**배포는 "완성 후 한 번"이 아니다.** 이 단계를 갖췄으므로 그 뒤로 수정은
`푸시 → 서버에서 한 줄`이 된다. 5단계를 먼저 하는 이유이기도 하다 — 그 뒤의
6·3단계는 만들면서 바로바로 올리는 형태가 된다.

### 3단계 — 기능 확장 *(6단계 뒤에)*

- **3a. 매크로 지표** — 공용 데이터. 갱신 주기가 시세와 다르다(월·분기). **기획 확정 — 아래 상세.**
- **3b. 재무 지표** — 공용 데이터. **함정이 있다:** 3분기 실적은 11월에 공시된다.
  이걸 "9월 30일 데이터"로 저장하고 과거를 검증하면 *그 시점에 알 수 없던 정보로
  판단한 셈*이 되어 결과가 실제보다 좋게 나온다. **기준 시점과 공시 시점을 둘 다
  저장해야 한다.** 나중에 고치려면 데이터를 다시 받아야 하므로 만들 때 넣는다.
- **3c. AI 종목분석** — 사용자 개인 API 키.
  - **키를 서버에 저장하지 않는다.** 남의 키를 우리 DB에 두는 순간 책임이 생기고,
    "암호화 저장"은 복호화 키가 같은 서버에 있어 근본 해결이 아니다. 아래 상세 참고.
    **"사용자별로 쓰려면 서버에 저장해야 한다"는 것은 사실이 아니다** — 각자 자기 키를
    쓰는 데는 브라우저 보관으로 충분하다.
  - 제공자별 API 차이는 **시세 제공자와 같은 패턴**으로 추상화한다 (`providers/`).
  - 에러를 번역한다 — 키 오류/잔액 부족/모델 권한 없음은 서로 다른 문제다.
  - **AI는 부가 기능**으로 둔다. 키 발급은 진입 장벽이므로, 키 없이도 나머지 기능은
    전부 동작해야 한다.
  - 프롬프트는 "판단"이 아니라 **"정리·설명"**으로 묶는다 (아래 규제 항목 참고).

#### 3c 상세 — AI 키를 어디에 두나 *(4단계와 함께 정한다)*

**먼저 틀린 전제 하나를 치운다.** "사용자별로 쓰려면 키를 서버에 남겨야 한다"는 사실이
아니다. *사용자별*이란 각자 자기 키로 호출한다는 뜻이고, 그건 키가 브라우저에 있어도
그대로 성립한다. 서버 보관이 실제로 사는 것은 **둘뿐**이다 — 기기 간 동기화, 그리고
**사용자가 앱을 안 열어도 서버가 대신 호출하는 것**(새벽에 미리 분석해 알림으로 보내는
따위). 두 번째가 필요한지가 진짜 분기점이고, 필요 없다면 이 논쟁 자체가 없어진다.

| | 키가 있는 곳 | 기기 간 | 서버가 대신 호출 | 서버가 털리면 |
| --- | --- | --- | --- | --- |
| **A. 브라우저 보관 + 브라우저가 직접 호출** | 브라우저 | ✗ | ✗ | 아무 일 없음 |
| **B. 브라우저 보관 + 서버는 중계만 (저장 안 함)** | 브라우저, 요청마다 전달 | ✗ | ✗ | 그 순간 오가던 것만 |
| **C. 서버 보관 (암호화)** | 서버 DB | ✓ | ✓ | **전원의 키** |
| **D. 서버 보관 + 서버는 못 읽음** | 서버 DB(암호문), 열쇠는 사용자 머릿속 | ✓ | ✗ | 암호문뿐 |

**C의 위험은 "얼마나 크냐"보다 "누가 책임지냐"다.** 털렸을 때 나가는 것은 남의 **결제
수단**이고, 한도를 안 걸어둔 키면 피해가 즉시 돈이 된다. 확률 자체는 낮겠지만(무료 티어
서버 한 대, 혼자 운영), 그 확률을 내가 감당하겠다고 남 대신 결정하는 셈이 된다.

그리고 C를 고르면 **키가 백업 파일에도 복사된다.** 지금 백업은 7벌 보관하고 주인이 자기
PC로 내려받을 수 있다 — 그 순간 남의 키가 내 PC에 있다. 로그에 실수로 찍히는 경로도 하나
더 생긴다. "DB만 안 털리면 된다"가 아니다.

**결정:** 3c는 **B로 시작한다.** 브라우저에서 제공자를 직접 부르는 A는 제공자마다 브라우저
호출을 막거나 별도 opt-in을 요구할 수 있어(3c 시작 시점에 제공자별로 확인) 깨질 수 있는
반면, B는 그 문제를 피하면서도 저장은 하지 않는다. B의 조건은 셋이다 — **로그에 키를 절대
남기지 않는다**(오류 리포트 포함), **메모리 밖으로 내보내지 않는다**, **HTTPS 전제**.

기기 간 동기화가 정말 필요해지면 **D**로 간다 (브라우저에서 사용자 패스프레이즈로 암호화해
서버에는 암호문만 둔다). 서버는 못 읽고 동기화는 된다. 대가는 패스프레이즈를 잊으면 복구
불가라는 것과 구현이 늘어나는 것이다.

**C는 "서버가 사용자 없이 AI를 호출해야 하는 기능"을 하기로 결정할 때만 다시 꺼낸다.**
그때도 기술 문제보다 먼저 볼 것이 있다 — 3절에서 위험을 낮추는 요소로 적은 것 중 하나가
*"AI는 사용자 본인 키, 우리가 조언을 생산해 배달하지 않는다"*였다. 서버가 남의 키로 분석을
돌려 알림으로 보내기 시작하면 그 구도가 흔들린다. 성격이 바뀌는 변경이다.

#### 3a 상세 — 매크로 지표 *(기획 확정)*

**무엇에 답하는 화면인가.** "지금 이 하락이 사도 되는 하락인가, 피해야 하는 국면인가."
무릎매수는 떨어질 때 사는 전략이라 *어떤 하락이냐*가 결정적이다. 매크로는 그 배경을
깔아주는 역할이다.

**시그널 조건에는 섞지 않는다.** 이게 이 절에서 가장 중요한 결정이다. 이유 셋:

1. `SIGNAL_APP_SPEC.md`의 백테스트가 검증한 것은 기술적 조건 네 개다. 매크로를 AND로
   걸면 **그 백테스트가 검증한 물건이 아니게 된다** — 연 5~9회라는 발동 횟수도 무의미해진다.
2. 적립식 원칙과 정면으로 충돌한다. "매크로가 나쁘니 이번 달은 쉰다"는 마켓타이밍인데,
   이 앱은 그걸 안 하기로 하고 **폴백 매수**를 만들었다.
3. 매크로가 나쁠 때가 보통 싸게 사는 때다. 조건으로 걸면 가장 좋은 기회를 스스로 막는다.

맥락으로만 쓴다 — 국면 배지를 보여주고, 판단은 사람이 한다.

##### 지표

| 지표 | 출처 | 주기 |
| --- | --- | --- |
| VIX | 야후 `^VIX` | 일 |
| 공포·탐욕 지수 | CNN `fearandgreed` | 일 |
| 미 10년물 금리 | FRED `DGS10` | 일 |
| 미 2년물 금리 | FRED `DGS2` | 일 |
| **근원 PCE** | FRED `PCEPILFE` | 월 |
| PCE 물가 | FRED `PCEPI` | 월 |
| 근원 CPI | FRED `CPILFESL` | 월 |
| CPI | FRED `CPIAUCSL` | 월 |
| 미 기준금리 | FRED `DFF` | 일(사실상 FOMC) |

금리차(10년−2년)는 **두 값에서 계산한다.** 따로 받으면 세 값이 서로 안 맞는 날이 생긴다.

공포·탐욕 지수는 3a-3에서 더했다. VIX와 겹쳐 보이지만 **같은 것을 재지 않는다** — VIX는
옵션 가격에서 나오는 숫자 하나이고, 이쪽은 일곱 가지(주가 모멘텀·신고가/신저가·시장 폭·
풋콜 비율·정크본드 수요·변동성·안전자산 수요)를 합친 0~100점이다. 둘이 어긋날 때가 오히려
볼 만하다.

다만 이 지표만 사정이 둘 다르다. **폴백이 없고**(이 지수를 내는 곳이 CNN뿐이다 — 이름이
같은 alternative.me 쪽은 암호화폐 시장 것이라 꽂으면 값이 조용히 다른 시장의 것으로 바뀐다),
**공식 API가 아니다**(지수 화면이 쓰는 주소를 그대로 부른다). 그래서 저쪽이 모양을 바꾸면
여기만 멈춘다.

셋째로 **과거가 짧다.** 다른 지표는 20년치를 받아두는데 이 지수는 2021년 언저리부터다.
없는 기간을 물으면 "그 기간은 없다"가 아니라 HTTP 500 이 오므로(실제로 그래서 지표가
통째로 안 들어왔다) 제공자가 요청 범위를 잘라서 보낸다. 지표마다 제공자를 따로 들고 있으므로 그 멈춤은 이 카드 하나로 끝나고,
`diagnose.py` 10번이 그때 무슨 일인지 말해준다.

근원 PCE를 목록 맨 위에 둔 이유: **연준이 실제로 목표 삼는 물가지표가 이것**이다.
뉴스에 나오는 건 CPI지만 금리를 정하는 건 PCE다.

##### 출처 — 키가 없어도 돈다

FRED(세인트루이스 연준)가 표준이다. Investing·트레이딩뷰는 쓰지 않는다 — 공개 API가
없고, 스크레이핑은 약관 위반이며 실제로 차단당한다. 무엇보다 **둘 다 원 출처가 아니다**
(CPI는 BLS, PCE는 BEA가 발표하고 FRED가 모은다). 중간상을 거칠 이유가 없다.

시세 제공자와 같은 폴백 구조를 쓴다 (`services/providers/fred.py`):

```
FRED API   (키 있으면 — 공식, 발표 일정까지 받을 수 있다)
  ↓ 키 없거나 실패
FRED CSV   (fredgraph.csv — 키 불필요, 공식 API는 아니다)
  ↓ 실패
야후       (^VIX 처럼 지수 형태인 것만)
```

AI 키와 같은 원칙이다 — **키 발급은 진입 장벽이므로, 키가 없어도 나머지는 전부 동작해야
한다.** 키가 있을 때만 되는 것은 "다음 발표일" 하나뿐이다.

##### 수정(revision) 함정 — 컬럼을 처음부터 넣는다

3b(재무)에 적어둔 함정이 매크로에 더 심하게 있다. **CPI·PCE는 발표 후에도 수정된다.**
8월 PCE가 9월에 2.8%로 나왔다가 10월에 2.9%로 고쳐지는 일이 흔하다.

지금은 "현재 상황 파악"이 목적이라 최신값이면 된다. 문제는 나중에 "매크로가 나빴을 때
무릎매수 성적이 어땠나"를 검증할 때다. 수정된 값으로 과거를 판단하면 *그 시점에 알 수
없던 정보로 판단한 셈*이 되어 결과가 실제보다 좋게 나온다.

그래서 `as_of`(언제의 값인가)와 `released_at`(언제 공개됐나)을 **둘 다 처음부터 넣는다.**
지금 아무 데도 안 쓰이지만, 나중에 추가하면 데이터를 전부 다시 받아야 한다. 컬럼 하나는
매우 싸다.

##### 컨센서스 — 세 갈래로 채운다

예상치가 없으면 "CPI 3.1%"가 좋은 숫자인지 나쁜 숫자인지 모른다. 시장이 움직이는 건
**예상 대비 어긋난 폭**이다. 그런데 블룸버그·로이터가 설문으로 만드는 그 컨센서스는
유료다. 같은 목적을 이렇게 채운다:

1. **클리블랜드 연준 인플레이션 나우캐스트** *(자동·무료·공식)* — CPI·근원CPI·PCE·근원PCE를
   발표 전에 **매일** 추정한다. 설문이 아니라 모델이라 숫자가 Investing 컨센서스와 똑같진
   않지만, 매일 갱신된다는 점에서 오히려 낫다. 1차 지표가 물가 계열 넷이라 커버리지가
   거의 정확히 맞는다.
2. **직접 입력** *(수동)* — Investing 화면에서 본 예상치를 한 줄 적어 넣는다. 스크레이핑이
   아니라 사람이 옮겨 적는 것이라 약관 문제가 없고, 한 달에 두 번이면 끝난다. 자동값보다
   우선한다.
3. **유료 API** — 자리만 만들어 둔다. 제공자 계층이 분리돼 있으니 나중에 갈아끼우면 된다.
   (네이버·스투크를 나중에 붙일 수 있었던 것과 같은 이유다.)

금리는 **FOMC 점도표(SEP)** 가 분기마다 FRED에 올라온다.

##### 테이블 셋

```
macro_series          # 지표가 뭔지 (공용)
  code PK, name, source, source_code, unit,
  frequency(daily|weekly|monthly|quarterly), display_order, active

macro_value           # 실제값 (공용)
  (code, as_of) UNIQUE
  value, released_at, fetched_at

macro_forecast        # 예측치 (공용)
  (code, as_of, source, forecast_date) UNIQUE
  value
```

지표를 늘릴 때 **코드를 안 고치고 `macro_series`에 행만 추가**하면 되게 쪼갰다. 프런트도
이 표를 읽어 화면을 만든다.

`forecast_date`를 따로 두는 이유: 나우캐스트는 **매일 바뀐다.** 한 칸에 덮어쓰면 "발표
직전에 시장이 뭘 예상하고 있었나"가 사라진다. 날짜별로 쌓으면 예상이 어떻게 움직였는지도
볼 수 있고, 저장 비용은 거의 없다.

**원본 지수를 저장하고 전년비·전월비는 우리가 계산한다.** CPI·PCE는 지수 형태로 온다
(`320.541`). FRED API에 변환 옵션이 있지만 쓰지 않는다 — 원본을 쥐고 있으면 나중에 다른
계산(3개월 연율 같은 것)이 필요할 때 데이터를 다시 안 받아도 되고, 제공자가 바뀌어도
저장된 값의 의미가 안 변한다. 시세를 다룰 때와 같은 이유다.

##### 갱신은 주기를 보고 고른다

매일 도는 배치가 `frequency`와 마지막 `as_of`를 비교해 **받을 때가 된 것만** 요청한다.
전부 매일 받으면 월간 지표를 한 달에 30번 두드리게 되고, 그건 차단당하는 지름길이다.

##### 화면

**매크로 탭** — 상단에 국면 배지, 아래에 지표 카드. 카드를 누르면 장기 차트(기존
`HistoryChart` 패턴 재사용). 예측치 직접 입력도 이 카드에서 한다.

배지 규칙은 전부 한 줄로 설명된다:

| 조건 | 배지 | |
| --- | --- | --- |
| 10년물 − 2년물 < 0 | 장단기 금리 역전 | **done** |
| VIX > 30 | 공포 구간 | **done** |
| 공포·탐욕 지수가 양 극단 | 극단적 공포 / 극단적 탐욕 | **done** |
| 물가가 예상을 상회 | 물가 상회 | **done** |

**합성 점수는 만들지 않는다.** "매크로 점수 62점"을 만드는 순간 왜 62인지 아무도 모르게
되고, 근거가 안 보이는 숫자는 판단에 도움이 안 된다. 배지를 따로 두면 이유가 그대로 보인다.

배지에는 **왜 떴는지가 같이 붙는다** (`VIX 32.4 (기준 30 초과)`). 이름만 있으면 −0.02%p
역전과 −1.5%p 역전이 같은 배지로 보이는데, 그 둘은 전혀 다른 상황이다.

색은 **전부 amber 하나**다. 빨강/초록을 쓰면 "역전됐으니 팔아라"로 읽히는데, 장단기 금리
역전은 과거에 침체보다 1~2년 앞섰고 그 사이 주가가 더 오른 적도 많다. 배지가 하는 말은
"이건 보고 가라"까지다.

"물가가 예상을 상회"는 **예상치가 들어와 있는 달에만** 판정한다. 없으면 그냥 안 뜬다 —
우리가 정한 기준(2% 목표 같은 것)과 비교하면 그건 출처가 없는 판정이고, 화면이 그걸
출처인 척 말하게 된다.

지표마다 따로 뜬다. CPI 와 근원 PCE 가 같은 달에 둘 다 예상을 넘었으면 그건 두 개의
사실이고, 합치면 어느 쪽이 얼마나 넘었는지가 사라진다.

**비교는 소수 첫째 자리에서 한다.** 발표도 예상도 거기까지만 말하는데("2.9%", "예상
2.7%"), 우리가 화면에 쓰는 전년비는 지수에서 직접 계산한 값이라 2.8734... 처럼 나온다.
반올림 전으로 비교하면 예상 2.7 에 실제 2.7049 인 달에도 배지가 뜨는데, 그건 상회가
아니라 우리 계산의 꼬리다.

##### 구간 이름

공포·탐욕 지수는 점수만으로 읽히지 않는다 — 33.7이 높은 건지 낮은 건지는 CNN이 정해둔
구간(0~24 극단적 공포 / 25~44 공포 / 45~55 중립 / 56~75 탐욕 / 76~100 극단적 탐욕)을
알아야 보인다. 그래서 카드에 **구간 이름과 그 범위를 같이 적는다.**

경계는 **서버가 들고 있다**(`services/regime.py`). 매크로 탭과 홈이 각자 25/45/55/75를
들고 있으면 언젠가 한쪽만 고쳐져서 같은 숫자를 놓고 둘이 다른 이름을 말한다.

구간이 있는 지표에만 붙는다. 금리 4.2%가 어느 "구간"인지는 아무도 정해놓지 않았고,
우리가 정하면 그건 출처 없는 판정이 된다.

발표 지표는 카드 모양이 다르다. VIX는 "지금 얼마"가 전부지만, CPI는 **언제 기준 값이고,
언제 발표됐고, 다음은 언제인가**가 같이 있어야 의미가 생긴다.

```
근원 PCE · 연준이 보는 물가
  2.8%  전년 대비        예상 2.9%  ▼ 0.1%p 하회
  2026년 8월 기준 · 9월 26일 발표 · 다음 10월 31일
```

`예상 2.9 / 실제 2.8 → 하회` — 이 한 줄이 컨센서스를 넣는 이유 전부다.

##### 홈 화면 즐겨찾기

고른 매크로 지표를 대시보드 상단에도 띄운다. 매크로 탭의 카드에서 ☆로 켜고 끈다.

저장 자리는 `portfolio_settings.pinned_macro` (JSON 배열, 순서 있음). **`macro_series`에
두면 안 된다** — 그쪽은 공용 데이터이고, 거기에 사용자별 선택을 섞는 것이 1절에서 지적한
`stocks` 테이블의 실수와 똑같은 종류다. `fx_overrides` 옆에 두면 4단계에서 `UserSettings`로
같이 따라간다.

**`NULL`과 `[]`를 구분한다.**

| 값 | 뜻 | 화면 |
| --- | --- | --- |
| `NULL` | 아직 안 건드렸다 | 기본값 (VIX · 장단기 금리차 · 공포·탐욕 지수) |
| `[]` | **일부러 다 껐다** | 아무것도 안 보인다 |
| `["VIX", ...]` | 고른 것 | 순서대로 |

둘을 합치면 "다 끄고 싶다"는 선택이 불가능해진다 — 껐는데 기본값이 다시 뜨는 화면은
고장으로 보인다.

기본값 셋은 성격이 겹치지 않게 골랐다: 공포(VIX) · 금리 곡선(장단기 금리차) · 심리(공포·탐욕
지수). 셋 다 **매일 움직이고**, 셋 다 국면 배지가 보는 값이라 홈에서 배지와 숫자가 맞물린다.
물가는 한 달에 한 번 바뀌어서 홈에 늘 띄워둘 값이 아니다 — 매크로 탭에서 보면 된다.

**장단기 금리차도 ☆로 켜고 끈다.** `macro_series`에 행이 없는 계산값이지만
(`TERM_SPREAD`), 사용자에게는 "볼 수 있는 것" 중 하나이지 우리 내부 사정이 아니다.
저장은 다른 코드와 똑같이 `pinned_macro` 배열에 들어간다.

##### 홈에도 뜨는 국면 배지

배지는 즐겨찾기 줄 **위에** 같이 뜬다. 걸리는 규칙이 없으면 아무것도 안 그린다 — 매크로
탭에서는 "눈에 띄는 국면이 없다"고 적지만(그 화면은 국면을 보러 온 곳이다), 홈에서는 그
한 줄이 종목 표를 밀어내는 값만큼의 값어치가 없다.

**배지는 고른 지표만 보고 만들지 않는다.** VIX를 홈에서 내렸다고 공포 구간 배지가
사라지면 화면이 "지금 조용하다"고 거짓말을 하게 된다. 그래서 홈은 고른 지표에 더해
규칙이 보는 지표(`regime.WATCHED_CODES`)까지 읽는다 — 전 지표를 읽는 것과는 다르다.

폰 화면이 좁으므로 홈의 매크로 줄은 **가로 스크롤 한 줄**로 둔다. 종목 표를 밀어내지 않는
것이 우선이다 — 홈의 주인공은 여전히 종목이다.

##### 예측치를 어디에 어떻게 넣나

입력은 **카드 안에서** 한다. 설정 화면으로 보내면 어떤 값이 나왔는지 보면서 적을 수가
없는데, 예상치는 그 값 바로 옆에서만 뜻이 있다.

넣을 수 있는 지표는 **발표되는 것**뿐이다(월간·분기). VIX·금리·공포탐욕은 시장에서 매일
나오는 값이라 발표일도 컨센서스도 없고, "예상 대비"라는 개념 자체가 없다.

**저장 단위가 원본과 다르다.** `macro_value` 에는 원본 지수(CPI 320.541)가 들어가지만
`macro_forecast` 에는 화면에 뜨는 전년비(2.7)가 들어간다. 지수 레벨로 예상치를 내는 곳이
없기 때문이다 — 사람이 Investing 에서 옮겨 적는 것은 전년비이고, 나우캐스트는 **전월비**를
낸다. 비교하는 자리에서는 단위가 하나인 것이 가장 중요하므로, 들어올 때 화면 단위로 맞춘다.

**칸이 둘이다.** 방금 나온 값의 예상치(`forecast`)와 아직 안 나온 달의 예상치
(`pending_forecast`). 한 칸에 합치면 하나가 다른 하나를 가린다 — 9월 예상치를 넣는
순간 8월 발표가 예상을 넘었다는 표시가 사라지거나, 반대로 방금 넣은 9월 예상치가
화면에 안 떠서 저장이 안 된 것처럼 보인다.

**지우는 길이 있어야 한다.** 같은 날 다시 넣으면 덮어쓰지만, 날이 바뀌면 줄이 쌓인다
(나우캐스트는 매일 바뀌고, 한 칸에 덮어쓰면 "발표 직전에 무엇을 예상했나"가 사라진다).
그래서 잘못 넣은 예상치는 덮어쓰기만으로는 못 지우고, 그동안 "물가 상회" 배지가 계속
떠 있게 된다. `DELETE` 가 그 달 **직접 입력분만** 지운다 — 받아온 값은 어차피 다음
배치가 다시 받아온다.

전년비는 **마이너스가 될 수 있다**(2009·2015년 CPI). 숫자 입력칸이 빼기표를 조용히
버리면 −0.2 를 넣었는데 0.2 가 저장되는데, 그건 거절이 아니라 **다른 숫자**라 화면만
보고는 알아채기 어렵다.

##### 나우캐스트 자동 수집 — 단위가 함정이다

데이터는 나우캐스팅 화면의 차트가 읽는 정적 파일에 있다:

```
/-/media/files/webcharts/inflationnowcasting/nowcast_{month,quarter,year}.json
```

**이 파일은 전월비(MoM)를 낸다.** 카드는 전년비로 뜨므로 그대로 넣으면 실제 3.2% 와
예상 0.44% 를 비교하게 되어 **매달 "물가 상회" 배지가 뜬다.** 숫자가 둘 다 그럴듯해서
화면만 보고는 알아챌 수 없다 — 야후 `^TNX` 를 10년물 폴백으로 쓰면 값이 조용히 열 배가
되는 것과 같은 종류다. 그래서 파일이 스스로 밝히는 단위(`yaxisname`)를 읽고, **모르는
단위면 받지 않는다.**

바꾸는 계산은 지어낸 것이 아니라 **실제값에 쓰는 것과 똑같은 변환**이다. 나우캐스트는
"이번 달 지수가 지난달보다 얼마 오를 것"이라 말하고, 지난달 지수와 열두 달 전 지수는
이미 우리가 들고 있다. 예상 지수를 만들어 `year_over_year` 와 같은 식으로 나눈다 —
카드의 3.2% 를 만드는 계산과 한 글자도 다르지 않다. 양쪽을 같은 계산으로 만드는 편이
저쪽의 전년비를 그냥 받아오는 것보다 낫다(그쪽은 자기 지수 판본으로 계산한다).

지난달 지수가 아직 없으면 **아무것도 만들지 않는다.** 매달 열흘쯤 생기는 상황이고
(9월분을 예측 중인데 8월분이 아직 발표 전), 그때 억지로 값을 만들면 그건 예상치가
아니라 우리가 지어낸 숫자다. 배지가 보는 것은 **이미 발표된 달**이라 그 달은 항상 바뀐다.

조용히 틀리기 쉬운 자리가 둘 더 있다:

- **발표일 세로선은 날이 아니다.** `categories` 목록 **가운데** `vline` 항목이 끼어
  있는데 값 쪽에는 그 자리가 없다. 안 거르면 그 뒤로 값이 전부 하루씩 밀린다.
- **같은 파일에 실제값이 나란히 있다** (`Actual CPI Inflation`). 예상치로 읽으면 예상과
  실제가 항상 같아져서 배지가 영영 안 뜨고, 그건 화면에서 안 보인다.

출처 우선순위는 **직접 입력이 자동값을 이긴다.** 직접 넣었다는 건 자동값이 마음에 안
들었다는 뜻이고, 그걸 자동값이 덮으면 다음 배치 때 조용히 되돌아간다.

7MB 짜리 파일이라 **하루에 한 번만** 실제로 받는다. 파일에는 2013년부터 들어 있지만
최근 24개월만 저장한다 — 다 넣으면 첫 실행에 수천 줄이 되고 화면에 쓰이는 것은 몇 달뿐이다.

##### 작업 순서

| | 내용 | 크기 | |
| --- | --- | --- | --- |
| 3a-1 | 테이블 셋 + 마이그레이션 + FRED 제공자(폴백) + 수집 | 중간 | **done** |
| 3a-2 | 주기별 갱신 스케줄 + 실패 처리 + `diagnose.py`에 매크로 항목 | 작음 | **done** |
| 3a-3 | API + 매크로 탭 화면 | 중간 | **done** |
| 3a-4a | 예측치 직접 입력 + "물가 상회" 배지 | 작음 | **done** |
| 3a-4b | 클리블랜드 연준 나우캐스트 자동 수집 | 작음 | **done** |
| 3a-5 | 국면 배지 + 홈 즐겨찾기 | 작음 | **done** |

개발 환경에서 FRED·야후가 **프록시 정책으로 차단**되므로(403), 테스트는 가짜 응답으로
여기서 만들고 **실제 조회 검증은 `diagnose.py`로 PC나 서버에서** 한다. 시세 제공자를
만들 때와 같은 방식이다.

### 4단계 — 사용자 분리 + 로그인 *(실제로 남을 받기로 할 때)*

혼자 쓰는 동안은 5단계의 비밀번호 하나로 충분하다. 이 단계는 **두 번째 사람이
들어오는 시점에** 한다. **기획 확정 — 아래 「4단계 상세」.** 로그인은 **구글만** 연다.

**구조를 쪼갠다** — 1절 "코드와 대조해보니"에서 찾은 것들이 전부 여기 들어온다.

- `stocks` → `Instrument`(공용) + `UserStock`(사용자별)
- `portfolio_settings` → `UserSettings`(사용자별) + 환율 캐시(공용)
- 공용 테이블 다섯 개(`price_daily`, `indicator_daily`, `signal_daily`, `buy_execution`,
  `holding`)의 외래키를 `Instrument`로 옮긴다. **옮기기 전에는 `purge`가 남의 시세를
  지운다.**
- 매일 갱신 대상을 "누구 하나라도 보고 있는 종목"의 합집합으로
- `holding`의 기본키를 `(user_id, ticker)`로. `buy_execution`의 중복 판정에도
  사용자를 넣는다

**로그인을 붙인다.**

- 구글 OAuth (비밀번호를 보관하지 않아도 된다)
- **개인정보처리방침** — 남의 계정 정보를 받는 순간 필요하고, 구글 OAuth 심사도 요구한다
- **모든 쿼리에 사용자 스코프.** 한 곳만 빠뜨려도 남의 데이터가 그대로 노출된다.
  눈으로 막을 수 있는 종류가 아니므로 **"A로 로그인해 B의 데이터가 안 보이는지"
  검사하는 테스트**를 따로 둔다.
- 로그·진단 화면의 잠금을 비밀번호에서 계정으로 바꾼다

#### 4단계 상세 — 구글 로그인 + 사용자 분리 *(기획 확정)*

**범위를 먼저 좁힌다.** 구글 하나만 연다. 네이버는 **계정 병합**을 먼저 정한 뒤에 붙인다 —
같은 사람이 구글로 가입했다가 다음에 네이버로 들어오면 자기 포트폴리오가 사라진 것처럼
보이는데, 이건 로그인 제공자를 하나 더 붙이는 일이 아니라 "한 사람이 누구인가"를 다시
정하는 일이다. 제공자를 나중에 더하는 건 싸고, 계정 병합을 나중에 하는 건 데이터가 쌓인
뒤라 비싸다.

그리고 이 단계는 **무료·지인 한정**을 전제로 한다 (3절 규제). 그 전제가 바뀌면 3절을 먼저
다시 본다.

##### 0. 이 단계를 관통하는 규칙 — 사용자가 없는 경로를 만들지 않는다

가장 중요한 결정이다. 모드를 셋으로 두되, **어느 모드에서도 요청에는 항상 사용자가 있다.**

| `DASHBOARD_PASSWORD` | `GOOGLE_CLIENT_ID` | 동작 |
| --- | --- | --- |
| 없음 | 없음 | 잠금 없음. 자동으로 **1번 사용자**로 들어온다 (개인 PC) |
| 있음 | 없음 | 비밀번호 한 문. 들어온 사람은 전부 **1번 사용자** (지금 서버) |
| 무관 | 있음 | 구글 로그인. 계정마다 다른 사용자 |

**구글이 설정되면 비밀번호 문은 닫는다.** 둘 다 열어두면 비밀번호를 아는 사람이 언제든
1번(주인) 계정으로 들어온다 — 계정을 나눈 의미가 그 문 하나로 없어진다.

`user_id`가 `None`일 수 있게 두면 **모든 쿼리가 "없으면 전체"라는 분기를 갖게 되고, 그
분기 하나하나가 남의 데이터가 새는 자리**가 된다. 로그인이 없는 개인 PC에서도 1번 사용자로
들어오게 해서 그 분기를 아예 없앤다.

##### 1. 표 — 무엇을 쪼개고 무엇을 그대로 두나

| 표 | 종류 | 하는 일 |
| --- | --- | --- |
| `user` *(새)* | — | `id`, `google_sub`(유일, 로컬 계정은 비어 있음), `email`, `name`, `created_at`, `last_login_at`, `session_epoch`, `is_owner` |
| `instrument` *(새)* | 공용 | `ticker` PK, `name`, `market`, `currency` — 지금 `stocks`의 공용 절반 |
| `user_stock` *(새)* | 사용자별 | `(user_id, ticker)` PK + `stocks`의 설정 컬럼 전부 (`active`, `category`, DCA, 목표비중, 밴드, 리뷰일, `sort_order`, `added_at`) |
| `user_settings` *(새)* | 사용자별 | `user_id` PK + `portfolio_settings`의 `default_rebalance_band_pct` · `base_currency` · `fx_overrides` · `pinned_macro` |
| `holding` | 사용자별 | PK를 `(user_id, ticker)`로 **바꾼다** |
| `buy_execution` | 사용자별 | `user_id` 추가. 중복 판정을 `(user_id, ticker, period_start, period_end)`로 |
| `price_daily` · `indicator_daily` · `signal_daily` | 공용 | 외래키를 `stocks.ticker` → `instrument.ticker`로 **옮긴다** |
| `fx_rate` · `macro_*` · `krx_listing` | 공용 | 그대로 |

**두 사람이 같은 종목을 동시에 추가하면** `instrument` 삽입이 충돌한다. `settings.py`가
이미 쓰는 방법 그대로 간다 — `IntegrityError`를 잡고 먼저 넣은 쪽의 행을 읽는다. 경합에서
지는 것은 실패가 아니다.

**식별 키는 구글의 `sub`이지 이메일이 아니다.** 이메일은 바뀔 수 있고(도메인 이전), 조직
계정은 회수돼 다른 사람에게 갈 수 있다. 이메일을 키로 쓰면 그날 남의 포트폴리오가 열린다.
`email`·`name`은 화면에 이름을 띄우기 위해서만 저장한다.

##### 2. 마이그레이션 0005 — 되돌릴 수 없는 유일한 리비전

순서는 이렇다. ①`user` 만들고 1번 넣기 → ②`instrument` 만들고 `stocks`에서 공용 4칸 복사
→ ③`user_stock` 만들고 나머지를 `user_id=1`로 복사 → ④`holding`·`buy_execution`에 `user_id`
추가하고 키 재구성 → ⑤`portfolio_settings` → `user_settings` → ⑥공용 3표의 외래키 교체 →
⑦`stocks`·`portfolio_settings` 삭제.

**함정 둘.**

- **SQLite는 기본키·외래키를 ALTER로 못 바꾼다.** 개인 PC는 SQLite다. Alembic의
  `batch_alter_table`(표를 새로 만들어 옮겨 담는다)로 가야 하고, 이건 Postgres 경로와
  코드가 갈린다. **CI의 두 갈래(SQLite·Postgres)에서 실제 데이터를 넣고 0005를 돌려본다.**
- **downgrade는 사용자가 한 명일 때만 가능하다.** 둘 이상이면 `user_stock` 여러 행을
  `stocks` 한 행으로 되돌릴 방법이 없다. 그러니 조용히 뭉개지 말고 **행 수를 세어 둘 이상이면
  거절**한다. 지워지는 걸 모르고 내리는 것보다 못 내리는 게 낫다.

마이그레이션 직전 백업은 이미 돈다(`app/migrate.py` → `backup.snapshot_before_migration`,
이미지에 `pg_dump`가 들어 있어 Postgres 도 뜬다). 다만 이번엔 **그 백업으로 되돌리는 절차를
실제로 한 번 해보고** `DEPLOY.md`에 적는다. 한 번도 복구해보지 않은 백업은 백업이 아니다.

**그리고 0005 만큼은 백업에 실패하면 멈춰야 한다.** 지금 `snapshot_before_migration`은
백업이 실패해도 경고만 남기고 진행한다 — 컬럼을 더하는 보통의 리비전에는 그게 맞는 기본값이다
(백업 때문에 앱이 안 뜨면 그게 더 나쁘다). 하지만 **0005는 되돌릴 수 없고, 앱이 뜨면서
자동으로 돈다.** 디스크가 찼거나 `pg_dump`가 없는 날 그대로 실행되면 돌아갈 자리가 없다.
0005 에서만 백업 성공을 조건으로 걸고, 배포 전에 손으로 한 벌 더 떠둔 뒤 시작한다.

##### 3. 수집 대상 — "누구 하나라도 보는 종목"

`pipeline.refresh_all_active_stocks`가 지금 보는 `Stock.active`는 "*내* 화면에서 치웠다"는
사용자별 뜻인데 공용 수집을 켜고 끈다. A가 치우면 B의 시세가 멈춘다.

`SELECT DISTINCT ticker FROM user_stock WHERE active`의 합집합으로 바꾼다. 아무도 안 보는
종목은 **갱신만 멈추고 데이터는 남긴다.** 남겨두면 누군가 그 종목을 다시 추가할 때 10년치
백필이 공짜가 된다 — **사용자가 늘수록 새 종목 추가가 빨라진다.**

`DELETE /{ticker}/purge`의 뜻도 바뀐다. 지금은 시세·지표까지 지우지만, 앞으로는 **내 목록에서
빼고 내 보유수량·매수기록만** 지운다. 공용 시세는 남의 것이기도 하다. 화면 문구도 같이 고친다
("시세와 기록까지 지웁니다"가 더 이상 사실이 아니다).

##### 4. 매수 워크플로우 — 종목 단위에서 사용자×종목 단위로

DCA 금액·주기가 사용자마다 다르므로 판정도 사용자마다 해야 한다. 비용을 미리 재봤다:
평가 한 번에 쿼리 3개, 기간 경계 계산은 600회에 190ms. **사용자 30명 × 20종목 = 1,800쿼리 ·
0.2초**다. 하루 한 번이므로 지금 구조 그대로 간다. 더 줄일 여지(시그널은 공용이므로 종목당
한 번만 읽고 메모리에서 사용자별 판정)는 남겨두되, 필요해지기 전에 하지 않는다.

##### 5. 구글 OAuth — 직접 만들지 않는 부분

- 흐름: `GET /api/auth/google/start` → `state`·`nonce`를 쿠키에 담고 구글로 302 →
  `GET /api/auth/google/callback?code&state` → 토큰 교환 → **`id_token` 검증** → 사용자 조회/생성
  → 세션 쿠키 → `/`로 302
- **`id_token` 서명 검증을 직접 구현하지 않는다.** `google-auth`의
  `verify_oauth2_token`을 쓴다(JWKS 회전·`aud`·`iss`·`exp`를 다 본다). 이 자리는 직접 짜면
  틀리고, 틀려도 화면에서는 멀쩡해 보인다.
- `state`는 필수다 — 없으면 남이 만든 로그인 링크로 **내 계정에 남의 구글 계정이 붙는다.**
- `redirect_uri`는 도메인 기준(`https://<도메인>/api/auth/google/callback`). 개인 PC에서
  시험하려면 `http://localhost:8000/...`도 콘솔에 같이 등록해둔다.

**문지기 통과 목록을 같이 고쳐야 한다.** `routers/auth.py`의 `PUBLIC_PATHS`에
`/api/auth/google/start`·`/callback`이 없으면 **로그인 자체가 401로 막힌다** — 아직 쪽지가
없는 사람만 지나는 길이기 때문이다. 문지기가 지키는 것이 `/api/` 전부라 놓치기 쉽다.

**세션 쪽지는 v2로 올린다.** 지금은 `v1.{만료}.{서명}`인데 `v2.{user_id}.{만료}.{서명}`으로
가고, 서명에 그 사용자의 `session_epoch`을 섞는다. 이 한 칸이 있어야 **탈퇴·강제 로그아웃**이
된다 — 서버가 세션 목록을 안 들고 있기 때문에, 그게 없으면 이미 나간 쪽지를 회수할 방법이
없다. 나중에 형식을 바꾸면 그 순간 전원이 로그아웃되므로 **지금 넣는다.** (v1 쪽지는 무효가
된다. 혼자 쓰는 지금은 한 번 다시 로그인하면 끝이다.)

##### 6. 가입은 기본이 닫혀 있어야 한다

공개 주소에 구글 로그인만 걸면 **전 세계 누구나 가입된다.** 무료 서버에 남의 데이터가
무제한으로 쌓이고, 그게 곧 책임이다.

- `ALLOWED_GOOGLE_EMAILS` (쉼표 구분)에 적힌 사람만 가입된다
- **비어 있으면 첫 로그인 한 명만** 가입되고 그 뒤는 거절한다

기본값이 위험한 쪽이면 안 된다 — `docker-compose.yml`이 비밀번호에 기본값을 안 주는 것과
같은 이유다.

##### 6-1. 권한은 둘뿐이다 — 주인과 사용자

**정해진 것:** 공용 데이터(매크로 지표·예상치·환율·상장목록·전체 갱신)는 **주인만**
고치고 새로고침한다. 사용자는 **자기 종목을 넣고 빼고, 자기 포트폴리오를 관리하고,
시그널을 본다.**

역할 표를 따로 만들지 않는다. `user.is_owner` 불리언 하나면 된다 — 지금 필요한 구분이
둘뿐인데 역할 테이블을 만들면 그 테이블도 지켜야 한다. 라우터에는 `require_owner`
의존성 하나를 건다.

| | 주인만 | 사용자 |
| --- | --- | --- |
| **공용 수집** | `stocks/refresh-all` · `macro/refresh` · `rebalance/fx/refresh` · `symbols/refresh-listing` | `stocks/{t}/refresh` — **내 종목만**, 티커당 쿨다운 |
| **공용 값 수정** | `macro/{code}/forecast` PUT·DELETE | — |
| **로그·진단** | `logs` · `logs/download` | — |
| **내 종목** | — | 추가·수정·순서·비활성·내 목록에서 제거 |
| **내 포트폴리오** | — | 보유수량 · 목표비중 · 밴드 · 리뷰일 · 기준통화 · 매수 확정 |
| **내 홈 화면** | — | 매크로 즐겨찾기(`macro/pinned`) |
| **읽기** | — | 대시보드 · **내 종목 차트** · 매크로 · 국면 배지 · 종목 검색 |

34개 엔드포인트 중 **주인 전용은 8개**다. 나머지는 사용자별이거나 공용 읽기다.

**사용자도 리밸런싱을 전부 쓴다 (확정).** 보유수량·목표비중·밴드·리뷰 마감일·매수 확정은
전부 사용자 몫이다. "사용자는 시그널만 본다"로 줄이는 안은 검토했다가 접었다 — 목표비중
대비 편차를 보고 조절하는 것이 이 도구가 하는 일의 절반이고, 그걸 빼면 남는 건 종목 목록에
배지가 붙은 화면뿐이다.

**즐겨찾기는 "지표 수정"이 아니다.** `macro/pinned`는 *내 홈 화면에 무엇을 둘지*이고
`user_settings`에 들어간다. 주인이 정해주면 남의 홈 화면을 주인이 꾸미는 게 된다.
보유수량·목표비중도 마찬가지다 — 그게 사용자가 이 도구를 쓰는 이유 자체다.

**화면에서 버튼을 숨기는 것으로는 안 된다.** 숨겨도 API는 열려 있다. 서버에서 막고,
화면은 *그래서* 숨긴다. 순서가 반대면 잠근 적이 없는 것과 같다.

###### 그런데 "종목 추가"는 외부 호출을 부른다

여기가 이 규칙의 유일한 구멍이다. 사용자가 **아무도 안 보던 종목**을 추가하면 10년치를
받아와야 한다(`POST /api/stocks` → `full_backfill`). "외부 호출은 주인만"을 글자 그대로
적용하면 사용자는 종목을 못 넣는데, 그게 사용자에게 주기로 한 기능의 전부다.

그래서 이렇게 가른다.

- **이미 누가 보고 있는 종목** → 외부 호출 **0회**. `user_stock`에 한 줄 넣으면 끝이고
  10년치 지표가 그 자리에서 다 보인다. 사람이 늘수록 이 경우가 늘어난다.
- **처음 들어오는 종목** → 그 종목 하나만 받아온다. 한 번이고, 그 뒤로는 공용 갱신이 맡는다.
- **상한을 둔다** — 사용자당 종목 수(예: 30개)와 하루 새 종목 등록 수. 한 사람이 200개를
  넣으면 **매일 갱신이 전원 몫으로 늘어나고 차단 위험도 같이 커진다.** 이건 악의가
  아니라 호기심으로도 일어난다.
- **내 종목 하나를 다시 받는 것은 사용자에게도 연다.** 갱신이 실패한 날이나 방금 등록한
  종목에서 사람이 제일 먼저 누르는 버튼이고, 그걸 막으면 할 수 있는 게 기다리는 것뿐이다.
  **다만 쿨다운은 사람이 아니라 종목에 건다** (예: 티커당 10분). 30명이 같은 VOO를 눌러도
  실제 조회는 10분에 한 번이므로 **외부 호출 총량이 사람 수와 무관해진다** — 1절의 경계가
  버튼에서도 그대로 유지되는 유일한 방법이다. 쿨다운 중에는 거절하지 말고 *"3분 전에
  받았습니다"*라고 사실을 알려준다.
  (전체 새로고침은 여전히 주인만이다. 그건 내 종목이 아니라 **모든 사람의 종목**을 건드린다.)
- **전체 백필(`?full=true`)은 주인만.** 같은 엔드포인트의 이 깃발 하나가 10년치를 통째로
  다시 받는다. 사용자에게 열어주는 것은 "최근 며칠 다시 받기"까지다.
- **종목 검색도 네트워크를 쓴다** — 로컬에서 못 찾으면 `symbols.search`가 **KRX 상장목록
  전체를 다시 받고**, 그래도 없으면 야후 검색을 부른다. 오타 하나가 목록 전체 다운로드를
  부르는 셈이라, 사람이 늘면 조용히 비싸진다. **여기도 자원에 건다** — 상장목록 갱신은
  전역 쿨다운(그 값은 모두에게 같다), 야후 검색은 사용자별 분당 몇 회. 로컬에서 찾히는
  평소 검색은 그대로 공짜다.
- **딱 하나 예외: 아직 한 줄도 못 받은 내 종목.** 등록할 때 조회가 실패해도 종목은 남는다
  (`create_stock`은 실패를 `data_loaded: false`로 돌려주고 행은 지우지 않는다 — 코드 주석도
  "이후 수동 새로고침으로 재시도"라고 적혀 있다). 그 버튼을 사용자에게서 빼면 **데이터 없는
  종목만 남고 손쓸 방법이 없다.** 그러니 경계를 이렇게 긋는다 — *남이 이미 가진 값을 다시
  받는 것*은 막고, *내 종목이 아직 비어 있는 것*은 허용한다. 시세가 한 줄이라도 있으면
  버튼은 사라진다. (매일 갱신도 어차피 다시 시도하지만, 그건 내일이다.)

###### 주인 계정을 구글 계정에 붙이는 일 — 안 하면 내 데이터가 사라진 것처럼 보인다

마이그레이션이 만드는 1번 사용자는 **구글 `sub`이 없는 로컬 계정**이다. 이 상태에서 내가
구글로 처음 로그인하면 **2번 사용자가 새로 생기고**, 내 종목·보유수량·매수기록은 전부 1번에
남는다. 화면은 빈 대시보드를 보여주고, 그건 고장으로 보인다.

`OWNER_GOOGLE_EMAIL`을 둔다. 그 이메일로 처음 로그인하면 **새 계정을 만들지 않고 1번에
`sub`을 연결한다**(1번에 아직 `sub`이 없을 때만). 이 한 칸이 없으면 전환 당일에 반드시
겪는다. 4-3의 확인 항목에 넣는다.

##### 7. 격리 테스트 — 사람의 주의력에 기대지 않는 방법

지금 API는 **34개**다. 그중 사용자별인 것에서 `WHERE user_id` 하나만 빠져도 남의 데이터가
그대로 나간다. 눈으로 막을 수 있는 종류가 아니다.

- 두 사용자 픽스처를 만든다 (A: VOO, B: 삼성전자). 각각 보유수량·매수기록·설정까지.
- **엔드포인트 표를 테스트가 들고 있고, 앱의 라우터 목록과 대조해 표에 없는 엔드포인트가
  생기면 실패한다.** 표의 칸은 둘이다 — *누구 것인가*(사용자별/공용)와 *누가 쓸 수
  있는가*(주인/사용자). 새 API를 만들면 둘 다 적어야 테스트가 통과한다. 새 API를 추가하면서 스코프를 잊는 것이 이 단계 이후 가장 흔한 사고인데,
  이 대조가 그걸 자동으로 잡는다.
- 각 항목은 "A의 쪽지로 B의 자원에 접근 → 404 또는 빈 목록"을 확인한다. **남의 자원은
  403이 아니라 404다** — 403은 "그런 게 있긴 하다"를 알려준다. 반대로 **주인 전용 동작**
  (전체 새로고침 같은)은 **403**이다. 그 엔드포인트가 있다는 건 비밀이 아니고, 404로
  돌려주면 "없는 기능"으로 읽혀 사용자가 엉뚱한 데서 원인을 찾는다.

**차트는 내 종목만 보인다.** `GET /api/history/{ticker}`는 `user_stock`에 그 티커가 있을
때만 답하고, 없으면 404다.

처음에는 "시세는 공용이고 티커는 비밀이 아니니 막지 말자"로 적었는데, 이건 **보안 문제가
아니라 화면 문제**다. 내가 등록하지도 않은 종목의 차트는 나에게 아무 뜻이 없고, 열 수 있다는
사실 자체가 "여기 남들이 뭘 보는지 들여다볼 수 있나"라는 인상을 준다. 공용으로 쓰는 것은
*저장과 수집*이지 *열람 범위*가 아니다 — **한 벌만 받아오는 것과 아무나 다 보는 것은 다른
얘기다.** 이 구분을 여기서 못 박는다.

잃는 것도 없다. 차트는 대시보드의 내 카드에서 여는 것이고, 등록 전 종목을 찾는 일은
종목 검색(`symbols/search`, 상장목록)이 맡는다.

##### 8. 남의 데이터가 새는 자리는 화면 밖에도 있다

- **로그**에 티커와 오류가 찍힌다. 사용자가 여럿이면 남의 종목이 내 로그에 보인다 →
  로그 보기·다운로드와 진단 화면은 `is_owner`만.
- **백업 파일**에 이제 남의 데이터가 들어간다 → 볼륨 권한, 그리고 보관 기간을 방침에 적는다.
- **탈퇴**(`DELETE /api/auth/me`)는 `user_stock`·`holding`·`buy_execution`·`user_settings`·
  `user`를 지우고 `session_epoch`을 올린다. 공용 데이터는 남는다 — 거기엔 그 사람을 가리키는
  게 없다.
- **개인정보처리방침·이용약관**이 필요하다. 구글 동의 화면이 URL을 요구하기도 한다. 수집
  항목(구글 식별자·이메일·이름), 목적(로그인), 보관(탈퇴 시 삭제), 제3자 제공 없음.

##### 8-1. 로그인이 데려오는 것, 안 데려오는 것

**계정을 따라오는 것** — 내 종목 목록과 순서, 보유수량, 목표비중·밴드·리뷰 마감일,
매수 기록, 기준통화·직접 입력 환율, 홈 화면 매크로 즐겨찾기. 폰에서 열든 회사 PC에서
열든 같은 화면이 뜬다. **이게 로그인을 붙이는 이유의 전부다.**

**따라오지 않는 것** — 테마(다크/라이트), 설치 안내 숨김, 그리고 **AI 키**(3c). 앞의 둘은
브라우저에 있어도 아쉬울 게 없지만 **AI 키는 다르다.** 키를 서버에 두지 않기로 한 결정의
대가가 "기기마다 한 번씩 넣어야 한다"이고, 설명이 없으면 그건 고장으로 보인다. 키 입력
칸 옆에 *"이 기기에만 저장됩니다 — 서버로 보내지 않습니다"*를 적는다.

##### 8-2. 사람이 늘면 같이 생기는 것들 (작지만 안 하면 걸린다)

- **매수 기록은 등록한 날부터 시작한다.** `tracking_start`가 `added_at`을 보므로, B가 오늘
  VOO를 넣으면 시그널 히스토리는 10년치가 다 보이는데 **매수 기록은 오늘부터**다. 지금도
  그런 규칙이지만 혼자 쓸 때는 눈에 안 띄었다. 화면에 한 줄 적는다.
- **첫 로그인은 빈 화면이다.** 지금 대시보드는 종목이 없으면 빈 상태를 보여준다. 나는 그게
  무슨 뜻인지 알지만 처음 들어온 사람은 모른다 — "종목을 추가해 보세요" 한 줄과 검색으로
  가는 길을 둔다.
- **누가 쓰고 있는지 볼 데가 없다.** 진단 화면(주인 전용)에 사용자 목록 한 줄씩 —
  이메일·마지막 접속·종목 수. DB를 열어보지 않고도 답할 수 있어야 하는 질문이다.
- **안 들어오는 계정의 종목도 계속 갱신된다.** 6개월 넘게 안 들어온 사용자의 종목을 수집
  합집합에서 뺄지 정해둔다(데이터는 남긴다). 사람이 몇 명일 때는 문제가 아니지만, 규칙을
  안 정해두면 영영 안 줄어든다.
- **같은 기기에서 계정을 바꿀 때.** 서비스 워커는 `/api/`를 캐시하지 않으므로 데이터는
  안 섞이지만(`sw.js`), 로그아웃 때 셸 캐시를 정리하는지는 4-3에서 확인한다.
- **푸시 알림(6단계)의 전제가 여기다.** 구독이 사용자별이 되므로, 푸시는 4단계 뒤에 붙인다.
  순서가 3 → 4 → 6(푸시)로 정리된다.
- **백업 파일을 내 PC로 내려받는 순간** 남의 데이터가 내 PC에 있다. 방침에 적고, 다운로드는
  꼭 필요할 때만. **백업을 되돌리면** 그 시점 이후 가입한 사람과 그 사이 입력한 보유수량이
  사라진다 — 혼자일 때는 내 문제였지만 이제 남의 기록이다. 복구 절차에 그 사실을 적는다.
- **거절당한 사람이 뭘 해야 하는지 화면에 없다.** 허용목록에 이메일을 넣는 건 주인인데,
  들어오려는 사람에게는 말할 창구가 없다. 로그인 거절 화면에 *"주인에게 문의"*와 연락
  수단 한 줄. 작지만 없으면 앱이 고장 난 것처럼 보인다.
- **무보증을 약관에 적는다.** 이건 취미로 돌리는 서버다. 며칠 멈출 수 있고, 데이터가
  사라질 수 있고, 시세는 무료 출처에서 온 참고값이다. AI 사용료는 각자 부담이다.
  **적어야 남에게 열 수 있다.**

##### 9. 쪼갠 순서 — 한 번에 하지 않는다

| | 하는 일 | 끝났을 때 확인 |
| --- | --- | --- |
| **4-0** ✅ | 테스트 픽스처를 한 군데로 모은다 (`tests/factories.py`) | 552개 테스트가 그대로 통과 |
| **4-1** ✅ | `instrument`/`user_stock`/`user_settings` 분리 + 0005. 로그인 없음, 전부 1번 사용자 | **겉보기 동작이 하나도 안 바뀐다** (화면·API 응답 동일) |
| **4-2** ✅ | 서비스·라우터에 `user_id` 전파 + 격리 테스트 골격 | 두 사용자를 DB에 직접 넣고 A/B가 안 섞이는지 |
| **4-3** ✅ | 구글 OAuth + 허용목록 + 주인 계정 연결 + 로그인 화면 + 탈퇴 | 실제 구글 계정 두 개로 로그인·탈퇴. **내 구글 계정이 1번에 붙는지** |
| **4-4a** ✅ | 주인(관리자) 전용 8개 잠그기 + 로그인 전 손님 둘러보기 | 사용자 쪽지로 주인 전용 API를 부르면 403, 남의 자원은 404. 손님은 매크로 읽기만 |
| **4-4b** | **가입 신청 → 관리자 승인**(사용자 목록: 승인·거절·차단, 앱 안 신청 표시) + 수집 합집합 + `purge` 의미 변경 + 종목 수 상한 + 새로고침 쿨다운·`full`은 관리자만·검색 횟수 제한 + 첫 화면 안내 + 방침 문서 | A가 종목을 치워도 B의 갱신이 도는지. 허용목록 없이 들어온 사람이 승인 전에는 아무것도 못 하는지 |

**4-0을 왜 따로 두나.** 지금 테스트는 `Stock(...)`을 **29곳**, `Holding(...)`·
`PortfolioSettings(...)`를 **13곳**에서 직접 만든다(6개 파일). 표를 쪼개면 이 42곳이 전부
깨지는데, 그걸 4-1 안에서 손으로 고치면 **테스트를 통과시키려고 테스트의 뜻을 바꾸게 된다** —
망가진 곳을 잡으라고 만든 그물을 망가진 뒤에 고치는 셈이다. 먼저 한 군데로 모아두면 4-1은
그 한 군데만 고친다.

**데이터 분리를 로그인보다 먼저 한다.** 로그인을 먼저 붙이면 계정은 여럿인데 데이터는
하나인 상태가 생기는데, 그 상태에서 누가 한 번 들어오면 서로의 포트폴리오를 덮어쓴다.
반대 순서에는 그런 중간 상태가 없다 — 4-1·4-2가 끝나도 혼자 쓰는 도구 그대로다.

##### 9-0. 4-1을 하면서 계획과 달라진 것 *(v0.17.0)*

- **표 이름은 `user`가 아니라 `users`다.** Postgres 예약어라 쓸 때마다 따옴표가 필요하다.
- **화면에 보이는 이름은 `user_stock.name`에 있다.** 계획표는 `name`을 `instrument`에만
  뒀는데, 이름은 사용자가 고쳐 쓰는 칸이다(야후의 영문 이름 → 한글). 공용에 두면 A가
  고친 이름이 B의 화면을 바꾼다. `instrument.name`은 처음 해석된 공식 이름으로 남는다.
- **`holding`·`buy_execution`의 외래키는 `(user_id, ticker)` → `user_stock`이다.** 내 목록에
  없는 종목의 보유·매수기록은 뜻이 없다. 종목을 빼려면 이 기록부터 치워야 한다.
- **`purge`는 4-4 전까지 지금 뜻을 지킨다** — 단, 공용 시세는 *아무도 안 담은 종목일
  때만* 지운다. 사람이 한 명인 지금은 예전과 똑같이 동작하고, 둘이 된 날 남의 시세를
  지우는 사고가 미리 막혀 있다.
- **외래키를 검사하지 않던 SQLite의 흔적**(종목만 지워지고 남은 시세·보유)을 0005가 만난다.
  시세는 공용이라 종목 행을 붙여 살리고, 보유·매수는 새 표에 들어갈 자리가 없어 옮기지
  않되 경고를 남긴다(원본은 직전 백업에). Postgres는 외래키를 늘 검사해서 해당 없다.
- **Alembic 이전 파일을 이어받는 길을 고쳤다.** 빠진 표를 *지금 모델*로 만들고 있었는데,
  0005 뒤로는 그 모양이 0001과 다르다(`holding`에 사용자 칸). 0001을 빈 메모리 DB에
  실제로 돌려 읽은 모양으로 만든다.
- **확인한 것**: 0004 모양에 데이터를 넣고 0005를 SQLite·Postgres 양쪽에서 돌려 값이
  하나도 안 바뀌는지, 한 명일 때 내렸다 다시 올리기, 두 명이면 내리기 거절, 옮긴 id 뒤로
  번호표가 가 있는지, 백업 실패 시 멈추는지. 그리고 예전 세션의 실제 DB 둘(0004 하나,
  Alembic 이전 하나)로 **옛 코드와 새 코드의 API 응답을 통째로 비교해 같았다.** 복구
  절차는 Postgres에서 직접 해보고 `DEPLOY.md` 10-1에 적었다.
- **아직 안 한 것 (4-2)**: 읽는 쿼리는 사용자로 거르지 않는다 — 사람이 한 명이라 지금은
  맞다. 새 행을 만들 때만 `LOCAL_USER_ID`(1)를 쓰고, 4-2에서 요청의 사용자로 바꾸면서
  모든 조회에 스코프를 건다. → **4-2에서 했다 (아래 9-0-1).**

##### 9-0-1. 4-2에서 한 것 *(v0.17.1)*

- **요청한 사람은 `current_user_id` 하나에서 나온다** (`services/users.py`). 지금은 늘
  1번이고, 4-3에서 세션 쪽지에서 꺼내도록 **이 함수만** 바뀐다. API 21곳이 이걸
  `Depends`로 받아 서비스에 넘긴다.
- **사용자별 서비스는 `user_id`를 기본값 없이 받는다** (`get_settings`, `fx.get_rates`,
  `compute_rebalance_current`, `macro.overview` …). 기본값이 있으면 넘기는 걸 잊어도
  1번으로 조용히 돌아가고, 그건 사람이 둘이 된 날 남의 데이터가 보이는 것으로
  드러난다. 없으면 잊은 자리가 그 자리에서 터진다 — 실제로 테스트 24곳이 그렇게 걸렸다.
- **종목 조회는 `user_stocks`/`find_user_stock` 두 함수로만 한다.** 남의 종목은 없는
  것과 같다(404). 매수 확정도 번호와 사용자를 같이 본다 — 번호는 전원이 같이 쓰는
  일련번호라 짐작할 수 있고, 번호만 보면 남의 매수가 **남의 보유수량에** 더해진다.
- **계획과 달라진 것 하나: 환율.** 직접 넣은 환율은 설정(사람마다)에 있는데, 공용
  갱신이 그걸 보고 "직접 넣은 통화는 안 받는다"로 건너뛰고 있었다. 사람이 여럿이면 한
  사람의 수동 입력 때문에 전원의 달러 환율이 멈춘다. 이제 공용 갱신은 늘 받고, 직접
  넣은 값은 그 사람 화면에서만 위에 얹힌다. 겉보기로는 같다.
- **격리 테스트** (`tests/test_isolation.py`):
  - API 34개 전부를 *누구 것인가*(사용자별·공용·공개)와 *누가 쓰는가*(주인·사용자·누구나)
    로 적은 표를 들고 있고, **앱의 라우터 목록과 양쪽으로 대조한다** — 표에 없는 API가
    생기거나, 사라진 API가 표에 남으면 실패한다.
  - 사용자별 20개마다 "B가 A의 것을 건드려 본다"는 확인이 붙어 있고, 없으면 실패한다.
  - A는 VOO·QQQ, B는 삼성전자·QQQ. **둘 다 QQQ를 담은 것이 핵심이다.** 사용자로 안
    거른 코드는 `{티커: 행}`으로 모으는데, 그러면 나중에 읽힌 행이 이겨서 **한쪽은
    우연히 맞는다.** 처음에 B만 확인했더니 누수 여덟 개 중 셋(보유수량 두 곳, 매수
    기록)을 못 잡았다. 읽는 확인은 A·B 둘 다 보게 고친 뒤 여덟 개를 하나씩 심어서
    전부 잡히는 것을 확인했다.
  - 주인 전용 8개는 표에 적어만 뒀다. 403으로 잠그는 것은 4-4다.
- **확인한 것**: SQLite 620 · Postgres 622 통과. 예전 세션의 실제 DB 둘로 4-1 코드와
  4-2 코드의 API 응답을 떠서 **바이트 단위로 같았다.**
- **4-4로 넘기는 것**: 전체 갱신·매일 갱신은 여전히 *담긴 줄마다* 돈다. 둘이 같은 QQQ를
  담으면 QQQ를 두 번 받는다 — 결과는 같고 호출만 두 배다. 사람이 한 명인 지금은 해당
  없고, 4-4의 "수집 합집합"이 이 자리다.

##### 9-0-2. 4-3에서 한 것, 계획과 달라진 것 *(v0.18.0)*

- **흐름은 계획 그대로**다 — `start` 가 `state`·`nonce` 를 쿠키에 담아 구글로, `callback` 이
  토큰을 바꾸고 `google-auth` 의 `verify_oauth2_token` 으로 서명·발급자·대상·만료를 본 뒤
  `nonce` 를 대조한다. **PKCE(S256)를 더했다** — 가로챈 `code` 를 다른 곳에서 토큰으로
  바꾸는 것까지 막는다. 흐름 쿠키는 서명하고 10분이 지나면 버린다.
- **달라진 것 1 — 주인 이메일은 필수다.** 계획은 "허용목록이 비면 첫 로그인 한 명만"이었는데,
  주인 연결과 합치면 그 한 명이 **1번(내 데이터 전부)** 에 붙는다. 공개 주소에서 누군가
  먼저 누르면 끝이다. 그래서 `OWNER_GOOGLE_EMAIL` 이 없으면 로그인 자체를 막고 화면에
  빠진 설정 이름을 띄운다. 허용목록이 비면 **주인만** 들어온다.
- **달라진 것 2 — 쪽지 서명에 구글 계정 식별자(`sub`)도 섞는다.** 계획은 `user_id`·만료·
  `session_epoch` 였다. 그런데 탈퇴하면 행이 지워지고, **SQLite 는 지워진 번호를 다음
  사람에게 다시 줄 수 있다.** 그러면 탈퇴한 사람의 쪽지가 그 번호를 받은 새 사람의 계정을
  연다 (epoch 는 둘 다 0). `sub` 를 섞으면 번호가 같아도 사람이 다르면 안 맞는다.
- **허용목록은 로그인할 때마다 본다** (가입할 때만이 아니라). 목록에서 빼면 다음 로그인부터
  막힌다. 이미 받은 쪽지는 만료(30일)까지 유효하다 — 바로 끊으려면 epoch 를 올린다
  (화면은 아직 없다. 4-4의 사용자 목록에 붙인다).
- **확인 안 된 이메일(`email_verified` 거짓)은 허용목록을 통과하지 못한다.** 남의 주소를
  적어 넣은 계정일 수 있다.
- **주인은 탈퇴할 수 없다.** 공용 데이터를 돌볼 사람이 없어진다.
- **테스트는 검증을 흉내 내지 않는다** (`tests/test_google_login.py`). RSA 키로 진짜 서명한
  `id_token` 을 쓰고 구글 공개키 주소만 바꿔 끼워, 서명·대상·발급자·만료 검증은
  `google-auth` 가 실제로 한다. 보호 장치 12개(대상·nonce·state·이메일 확인·서명의 sub·
  epoch·구글 모드의 비밀번호 쪽지·기존 사용자 허용목록·PKCE·주인 연결·흐름 쿠키 서명·
  주인 탈퇴)를 하나씩 빼서 **전부 테스트가 잡는 것을 확인했다.** 처음엔 둘을 못 잡았는데,
  하나는 테스트가 엉뚱한 이유(파이썬 인자 충돌)로 통과하고 있었다.
- **브라우저로 끝까지 돌려봤다.** 진짜 앱을 띄우고 구글 화면만 건너뛰게 해서 (콜백으로 곧장
  돌려보냄) PC·폰 폭에서 주인 로그인 → 기존 종목이 보임 → 나가기 → 낯선 계정 거절 안내 →
  친구 로그인(빈 화면) → 탈퇴까지. 가로 넘침·콘솔 오류 없음.
- **아직 안 한 것**: 실제 구글 계정으로 서버에서 로그인해 보는 것 (주인이 한다 — DEPLOY.md
  12번). 로그·진단·전체 갱신 같은 주인 전용 8개는 **아직 잠기지 않았다** — 허용목록에 넣은
  사람은 볼 수 있다. 4-4에서 403으로 잠근다. 그 전까지는 믿는 사람만 허용목록에 넣는다.
  → **주인이 실제 계정으로 로그인해 기존 데이터가 붙는 것을 확인했다.** 잠금은 4-4a에서 했다.

##### 9-0-3. 4-4a에서 한 것 — 손님과 관리자 *(v0.19.0)*

주인이 실계정으로 써 보고 두 가지를 요청했다: **먼저 페이지에 들어와 본 뒤 로그인 버튼으로
들어오게**, 그리고 **내 계정은 관리자, 나머지는 사용자.** 둘째는 계획의 "주인 전용 8개
잠그기" 그대로라 4-4를 쪼개 이것부터 했다. 화면 말로는 "주인" 대신 **"관리자"** 를 쓴다
(코드·설정 이름 `is_owner`·`OWNER_GOOGLE_EMAIL` 은 그대로).

- **구글 모드에서는 문을 세우지 않는다.** 로그인 전 손님도 화면에 들어온다. 처음 온 사람이
  무엇을 하는 도구인지 보지도 못하고 로그인부터 만나면 대개 돌아간다.
  - 손님이 보는 것은 **공용이고 읽기뿐인 것 — 매크로 지표뿐**이다 (`GET /api/macro*`).
    시세·시그널도 공용이지만 열지 않는다. 종목별로 열면 *누가 어떤 종목을 담았나* 가
    드러난다.
  - 손님의 즐겨찾기는 **기본 셋**이다. 1번의 설정을 빌려주면 관리자가 고른 것이 바깥에
    보인다. 손님은 설정 행도 만들지 않는다.
  - 대시보드 자리에는 매크로 한 줄 + "무엇을 하는 도구인지" + 로그인 버튼. 히스토리·
    리밸런싱·종목 관리 탭은 **보이되** 누르면 로그인 안내다 — 무엇이 있는지는 보여준다.
  - 거절 안내(`?login_error=`)는 헤더 아래 띠로 옮겼다. 탭을 옮겨도 남아 있다.
  - **비밀번호 문은 그대로 잠근다.** 한 사람의 서버라 문 앞에서 보여줄 것이 없다.
- **손님 문은 두 겹이다.** 문지기가 `GET` 만 통과시키고(`guest_can_read`), 라우트는 따로
  사람을 요구한다(`current_user_id`·`require_owner`). 지금은 한 겹만으로도 막히지만,
  `/api/macro` 아래에 사람을 안 받는 쓰기 API가 생기는 날 나머지 한 겹이 막는다. 둘을
  각각 테스트한다.
- **관리자 전용 8개는 `require_owner` 로 잠갔다** — 사용자는 403 ("관리자만 쓸 수 있는
  기능입니다"). 전체 새로고침, 매크로·환율·상장목록 받아오기, 예상치 넣기·지우기, 로그
  보기·내려받기. 전부 **모두가 같이 쓰는 것**을 건드린다. 화면에서도 그 버튼들이 사용자·
  손님에게는 안 보인다. 사용자의 시세는 장 마감 뒤 서버가 받는다 (전원의 종목을 돈다).
- **잠금 없는 PC·비밀번호 문은 늘 1번 = 관리자**라 혼자 쓰는 동안 달라지는 것이 없다.
- **나가기·탈퇴는 첫 화면을 새로 연다.** 화면마다 앞 사람의 종목을 들고 있어서, 하나라도
  비우는 걸 잊으면 같은 폰의 다음 사람에게 보인다. 새로 여는 것은 빠뜨릴 곳이 없다.
- **격리 표에 `손님` 칸을 더했다.** 앱의 모든 API를 로그인 전 손님으로 불러 표의 `손님`·
  `누구나` 만 지나가는지, 주인 전용 8개를 사용자로 불러 전부 403인지, 그리고 라우터를
  훑어 `require_owner` 가 **표에 주인 전용으로 적은 것에만, 전부** 붙어 있는지 본다.
- **확인한 것**: SQLite 657 · Postgres 659 통과, 화면 362. 보호 장치 14개(손님 문·GET 한정·
  비밀번호 문에 손님 없음·관리자 판정·라우트 하나 풀기·손님이 1번 즐겨찾기 빌리기·
  화면의 관리자 판정·로그인 안내·만료 시 손님으로·처음부터 잠그기·나갈 때 새로 열기·
  매크로 받아오기/별/예상치 버튼)를 하나씩 빼서 전부 테스트가 잡는 것을 봤다 (GET 한정은
  처음에 못 잡았다 — 라우트 쪽이 막고 있어서다. 문지기만 따로 보는 테스트를 더했다).
  브라우저로 PC·폰 폭에서 손님 → 낯선 계정 거절 → 관리자 → 나가기 → 사용자(403 넷) →
  탈퇴까지 돌렸다. 가로 넘침 없음, 앱이 스스로 낸 401·403 없음.
- **4-4b로 남긴 것**: 수집 합집합(둘이 같은 종목을 담으면 두 번 받는다), `purge` 의미 변경,
  종목 수 상한, 방침 문서, 관리자 화면의 사용자 목록(쪽지 끊기 포함).

##### 9-1. 사람이 늘면 서버가 버티나 — 재본 값

추측하지 않고 실제 스키마에 20종목 × 10년치(표마다 50,400행)를 넣고 앱을 띄워 쟀다
(Postgres 16, 4코어).

| 잰 것 | 값 |
| --- | --- |
| 종목 하나 10년치 (시세+지표+시그널) | **1.3 MB** |
| 대시보드 한 번 (20종목) | 쿼리 **14건** · 135 ms — N+1 없음 |
| 동시 1명 / 20명 / 50명 | 4.7 / 20.8 / 25.2 req/s (p95 0.26s / 1.4s / 2.1s) |
| 종목 하나 10년치 재계산 | 225~260 ms |
| 앱 프로세스 메모리 | 144 MB |

**저장량도 갱신 시간도 사람 수가 아니라 서로 다른 종목 수에 비례한다** — 1절의 경계가
사주는 것이 이것이다. 100명이 들어와도 보는 종목은 크게 겹치므로 서로 다른 티커는
200~300개쯤이고, 그건 **260~400MB · 재계산 80초**다. 사람 한 명이 늘 때 실제로 늘어나는
행은 100~200줄이다.

**그래서 자원은 100명까지 지금 서버 그대로다.** 먼저 깨지는 것은 RAM도 CPU도 아니라
**시세 제공자의 인내심**이다(4절). 그건 사람 수가 아니라 티커 수에 비례하고, 지금 구조에서는
차단되면 전원이 같이 멈춘다.

##### 10. 이 계획에서 가장 위험한 자리 넷

1. **0005 마이그레이션.** 되돌릴 수 없고, 실패하면 개인 PC DB가 망가진다. → 백업 복구를
   실제로 해보고, CI 두 갈래에서 데이터를 넣은 채로 돌려본다.
2. **스코프 누락.** 한 쿼리만 빠져도 남의 데이터다. → 7번의 전수 대조.
3. **공개 가입.** 기본값이 열려 있으면 하루 만에 모르는 계정이 쌓인다. → 6번.
4. **주인 계정이 안 붙는 것.** 구글 로그인을 켠 날 내 데이터가 사라진 것처럼 보인다.
   → 6-1의 `OWNER_GOOGLE_EMAIL`.

##### 11. 안 하기로 한 것

- **네이버 로그인** — 계정 병합을 정한 뒤에
- ~~**관리자 화면** — 허용목록은 환경변수로 충분하다.~~ **뒤집혔다 (2-2).** 사람을 받을 때마다
  `.env`를 고치고 서버를 다시 띄우는 게 실제로 번거로웠다. 4-4b에서 가입 신청 → 승인으로 간다
  (화면을 만들면 그 화면도 지켜야 한다는 값은 그대로 치른다 — 격리 표에 올린다)
- **사용자별 지표 파라미터** — 1절의 "비싼 개인화". 공용 `indicator_daily`가 깨진다
- **포트폴리오 공유·조직 계정** — 지금 필요 없고, 넣으면 격리 테스트가 몇 배가 된다

##### 12. 이 계획을 코드와 대조하면서 고친 것

초안을 쓰고 저장소와 맞춰보니 여섯 군데가 틀렸거나 비어 있었다. 남겨둔다 — 같은 종류의
누락을 다음에도 하기 때문이다.

| | 초안 | 고친 뒤 |
| --- | --- | --- |
| 1 | "4-1이 끝나면 기존 테스트가 그대로 통과한다" | **틀렸다.** 42곳이 모델을 직접 만든다 → 4-0을 앞에 둔다 |
| 2 | 격리 = 남의 것을 못 보는 것 | **읽기만으로 부족하다.** 예상치 입력이 전원의 배지를 바꾼다 → 공용 쓰기는 주인만 |
| 3 | 수집은 스케줄러만 생각했다 | **"지금 받아오기" 버튼 넷**이 사람 수만큼 외부 호출을 곱한다 |
| 4 | 비밀번호와 구글을 둘 다 열어둠 | 비밀번호 문이 주인 계정의 뒷문이 된다 → 구글이 켜지면 닫는다 |
| 5 | 종목 추가 경합을 안 봤다 | 두 사람이 같은 종목을 동시에 추가 → `settings.py`와 같은 패턴 |
| 6 | 공용 읽기를 어떻게 할지 안 정함 | 결정으로 적기로 했다 (**아래 7번에서 다시 뒤집힌다**) |

권한을 주인/사용자 둘로 정하고 나서 하나가 더 나왔다: **"외부 호출은 주인만"과 "사용자는
자기 종목을 넣는다"가 정면으로 부딪친다.** 새 종목 등록은 외부 호출을 부르기 때문이다.
6-1에서 갈라뒀다 — 이미 있는 종목이면 0회, 처음 들어오는 종목이면 한 번, 그리고 상한.

그리고 초안을 읽고 두 가지가 더 뒤집혔다.

| | 초안 | 고친 뒤 |
| --- | --- | --- |
| 7 | 히스토리는 공용이니 안 막는다 | **내 종목만.** 공용인 것은 *저장과 수집*이지 *열람 범위*가 아니다 |
| 8 | 개별 새로고침은 주인만 | **내 종목은 사용자도.** 대신 쿨다운을 사람이 아니라 **종목에 걸어** 총량을 사람 수와 무관하게 만든다 |

### 5단계 — 배포 *(코드는 끝났다. 남은 건 서버를 만드는 일)*

**코드 쪽은 v0.9.0에서 닫혔다.**

- ✅ **비밀번호 하나로 잠근다.** 공개된 주소에 그냥 올리면 **아무나 진단 화면과 로그를
  본다** — 종목·에러·내부 경로가 전부 찍힌다. `DASHBOARD_PASSWORD`를 채우면 화면과 API
  전체가 잠기고, 비워두면(개인 PC) 예전과 똑같이 동작한다. 서명된 쿠키라 서버가 세션
  목록을 들고 있지 않아 다시 띄워도 로그인이 유지되고, **비밀번호를 바꾸면 이미 나간
  쪽지가 전부 무효**가 된다.
  혼자 쓰는 동안 필요한 건 "아무나 못 들어온다"이지 사용자별 분리가 아니므로, 여기서는
  잠그기만 하고 계정은 4단계로 미룬다. (개인정보처리방침도 4단계로 함께 간다.)
- ✅ **스케줄러 중복 실행 방지.** Postgres 어드바이저리 락으로 한 프로세스만 맡는다.
  주석으로 "워커를 하나만 띄운다"고 적어두는 것은 `--workers 4`를 막지 못한다.
  락은 연결이 끊기면 저절로 풀려서 죽은 프로세스가 자물쇠를 쥔 채 남지 않는다.
- ✅ **도메인 + HTTPS.** Caddy를 compose 프로필로 붙였다(`--profile https`). 인증서는
  받는 것도 갱신도 알아서 한다 — 90일마다 사람이 기억해야 하는 일은 결국 잊어버리고
  그날 사이트가 죽는다.
- ✅ **절차를 적어뒀다** (`DEPLOY.md`). 오라클 인스턴스 만들기부터 첫 배포까지, 실제로
  막히는 자리(**방화벽이 두 겹**, ARM 용량 없음, 1GB에서 빌드가 죽는 것)를 포함해서.
- ✅ **서버에서 빌드하지 않는다.** ARM 재고가 없어 실제로 잡힌 것은 1GB짜리
  E2.1.Micro였고, 거기서는 화면 빌드가 OOM으로 죽는다 — 그것도 *이유 없이 멈춘 것*처럼.
  그래서 GitHub이 푸시마다 이미지를 만들어 GHCR에 올려두고 서버는 받기만 한다
  (`docker compose pull`). **1GB는 돌리는 데는 충분하다**(평상시 500~600MB) — 모자란 건
  빌드 순간뿐이었고, 그 순간을 서버 밖으로 옮겼다. 덤으로 업데이트가 10분대에서 1분
  안쪽이 되고, `.env`의 `IMAGE_TAG` 한 줄로 예전 커밋으로 되돌릴 수 있다.
  이미지 이름이 `docker-compose.yml`과 워크플로에서 갈라지면 **서버가 조용히 옛
  이미지를 계속 받아쓰므로**(오류가 안 난다) CI가 두 파일을 대조한다.
- 백업은 이미 네 갈래로 자동이다(2단계). 남은 것은 **서버 밖으로** 꺼내오는 일인데,
  지금은 수동(`DEPLOY.md` 10번)이다. 서버가 통째로 사라지는 경우만 다르므로 자동화는
  실제로 쓰기 시작한 뒤에 판단한다.

**남은 것은 내가 직접 해야 하는 일이다.** 오라클 계정으로 인스턴스를 만들고, 도메인을
사고, A 레코드를 연결하는 것 — 코드로 대신할 수 없다. 오라클 ARM은 **인기 리전에서
"용량 없음"이 잦다.** 항상 잡히는 AMD 마이크로는 RAM 1GB라 빌드가 죽을 수 있어 스왑이
필요하다. Docker라 다른 곳으로 옮기는 비용이 거의 없으니 막히면 갈아탄다.

### 6단계 — PWA

✅ **설치·오프라인·모바일은 끝났다 (v0.11.0).**

- manifest, 아이콘 다섯 벌, 서비스 워커, 설치 버튼
- 오프라인에서도 화면이 뜬다 (마지막에 받은 껍데기를 돌려준다)
- 모바일: 노치 회피, 손가락 크기 버튼, 입력칸을 눌러도 화면이 확대되지 않음

캐시 규칙은 **`/api/` 를 절대 건드리지 않는 것**이 핵심이다. 시세·시그널을 한 번이라도
캐시하면 "어제 값이 오늘 값인 척"하는데, 그 증상은 *가끔 숫자가 이상하다*로 나타나
원인을 찾기 대단히 어렵다. 그래서 규칙을 지키는지 테스트가 **실제 배포되는 sw.js 를
읽어다 실행해서** 확인한다 (`frontend/src/test/sw.test.ts`).

> **설치와 오프라인은 https 에서만 된다.** 브라우저 규칙이라 우회할 수 없다. 집 안에서
> `http://192.168.0.x:8000` 으로 들어가면 설치 버튼이 뜨지 않는다 — 고장이 아니다.
> 5단계에서 도메인을 붙이고 나면 그때부터 된다.

**남은 것 — 푸시 알림.** 이 단계의 핵심이자 아직 못 한 부분이다. 부록이 아니다. 이
도구의 성격상 매일 대시보드를 열어보는 사람은 적고, *"시그널이 떴습니다" 알림이 오면
여는 것*이 실제 사용 방식이 된다. 있고 없고가 "가끔 여는 사이트"와 "쓰는 앱"을 가른다.
서버(5단계)가 실제로 떠야 보낼 곳이 생기고, 아이폰은 **홈 화면에 추가한 경우에만**
받을 수 있다(iOS 16.4+). 그래서 설치를 먼저 했다.

> **뒤로가기로 팝업 닫기는 일부러 미뤘다.** 안드로이드에서 차트 팝업을 열고 뒤로가기를
> 누르면 팝업이 닫히는 게 자연스럽다. 구현하려면 팝업을 열 때 방문기록을 하나 밀어
> 넣어야 하는데, 그게 라우터의 기록과 엉키면 **탭 이동 자체가 이상해진다.** 얻는 것보다
> 잃을 수 있는 것이 커서, 푸시 알림과 같이 다룬다.

"앱처럼 보이는 완성도"는 배포 형태가 아니라 여기서 온다. exe로 감싼다고 해결되지
않고, 이게 잘 되어 있으면 대부분의 사용자는 PWA와 앱을 구분하지 못한다.

### 7단계 — 선택: 앱스토어

Capacitor로 **지금 React 코드를 그대로** 네이티브 껍데기에 넣는다. 서버는 어차피
있으니 버려지는 작업이 없다.

비용: Apple 연 $99 / Google 1회 $25, 심사 며칠, 그리고 **투자 관련 앱은 심사가 까다로운
편이다** (아래 규제 항목이 서류로 돌아온다). PWA로 먼저 실제로 쓰이는지 본 다음 결정한다.

> **PyInstaller exe / Electron은 하지 않는다.** 백엔드를 사용자 PC에 설치하는 방향이라,
> 서버에 올리는 순간 그 작업이 쓸모없어진다. 앱 경험은 PWA → Capacitor로 간다.

---

## 2-1. 중간점검에서 찾은 것 (v0.8.1)

5단계로 넘어가기 전에 코드를 한 번 훑었다. 여섯 개가 나왔고 **다섯은 닫았다.**

고친 것 — 셋 다 같은 이야기였다. *개인 PC는 꺼진다.*

- ✅ **켤 때 도는 작업이 9시간 뒤에 잡히고 있었다.** 스케줄러는 UTC로 도는데 시간대
  없는 `datetime.now()`를 줘서, APScheduler가 그걸 UTC로 읽었다. 한국(UTC+9)에서는
  "15초 뒤"가 9시간 뒤가 된다 — **켤 때 하는 백업과 상장목록 갱신이 한 번도 안 돌고
  있었다.** 만들어둔 기능이 조용히 죽어 있는 종류라, 로그에도 아무 흔적이 없었다.
- ✅ **갱신이 하루라도 밀리면 그 사이 시그널이 영영 기록되지 않았다.** 매수 판정이
  "마지막 시그널 날짜" 하루만 봤다. 이제 기간 전체에서 **처음** 뜬 날을 찾는다
  (등록일 이전으로는 가지 않는다 — 그때는 알 수도 없던 일이다).
- ✅ **켤 때 시세를 따라잡는 장치가 없었다.** cron은 놓친 실행을 되돌려주지 않는다.
  백업에는 이 장치가 있었는데(`backup_startup`) 시세에는 없어서, 며칠 꺼뒀다 켜면
  시세가 그대로였다. 뒤처진 **시장만** 골라 받아온다.

> 앞의 둘이 겹치면 이렇게 된다: PC를 며칠 꺼둔다 → 그동안 갱신이 안 돈다 → 켜서
> 새로고침해도 마지막 날만 판정된다 → **그 사이 매수 시그널이 통째로 사라진다.**
> 서버에 올리면 늘 켜져 있어 증상이 가려지므로, 배포 전에 고치는 게 맞았다.

- ✅ **Postgres를 한 번도 안 돌려본 상태였다.** 이제 CI가 같은 테스트를 SQLite와
  Postgres 양쪽에서 돌린다 (`tests/dbsetup.py`의 `TEST_DATABASE_URL` 하나로 갈린다).
  `0001_initial.py`가 Postgres에 실제로 적용되고 모델과 벌어지지 않는 것이 이제
  자동으로 확인된다.

  **붙이자마자 하나 잡았다.** SQLite는 **외래키를 아예 검사하지 않는다.** 그래서
  자식 행을 부모보다 먼저 넣어도 조용히 통과한다 — Postgres는 거절한다. 테스트
  하나가 정확히 그 상태였다.

  > **5단계에서 기억할 것:** 지금 쓰는 SQLite 파일에 부모 없는 행이 하나라도 있으면,
  > 그 데이터를 Postgres로 옮길 때 그 자리에서 막힌다. 옮기기 전에 확인한다.

- ✅ **`SIGNAL_APP_SPEC.md`를 되살렸다.** 코드 6곳과 README가 참조하는데 파일이 없었고
  git 이력에도 없었다. 구현에서 1:1로 복원했고, 지표 공식·경계값 21개가 코드와 맞는지
  대조해 확인했다.

  **복원되지 않은 것이 있다** — 원본의 백테스트 결과와, 임계값을 그 값으로 정한 근거다.
  그래서 이 문서는 *"지금 이렇게 동작한다"* 의 근거이지 *"이 값이 옳다"* 의 근거가 아니고,
  그 사실을 문서 1장에 박아뒀다. 임계값을 바꾸려 할 때 이 차이가 중요해진다.

  `test_spec.py`가 문서가 사라지거나 없는 장을 가리키는 것을 잡는다. 같은 일이 다시
  일어나도 이번엔 조용하지 않다.

남은 하나 — ~~3단계에서 한다~~ **v0.20.0에서 닫았다 (2-2).**

- ✅ **리밸런싱 이력이 남지 않는다.** 전부 실시간 계산이고 `Holding.quantity`는
  덮어쓰기라 과거 비중을 복원할 수 없다. 분기마다 리뷰하는 도구인데 지난 리뷰를 볼 수
  없다. **나중에 넣어도 그 전 기록은 못 살린다.**

  처음에는 3b(재무 지표)와 함께 하기로 했는데, 중간검토(2-2)에서 앞당겼다. 기다리는
  분기마다 기록 하나가 영영 사라지는데, 리뷰 기록은 재무처럼 "공시 시점" 같은 함정이 없어
  따로 해도 설계가 갈라지지 않는다. `rebalance_snapshot` — 리뷰할 때 누르는 기록.


---

## 2-2. 중간검토 — 포트폴리오를 단순하게, 남은 순서를 다시 (v0.20.0)

4-4a까지 끝낸 뒤 목표와 대조해 남은 일을 다시 봤다. 바뀐 것은 셋이다.

### 1. 매수 기록을 걷어냈다 — 리밸런싱에 필요한 건 수량뿐이었다

코드를 대조해 보니 **리밸런싱 계산은 매수 기록을 한 번도 읽지 않았다.** 쓰는 값은 수량 ×
현재가, 목표비중, 밴드, 리뷰일뿐이다. 종목마다 적던 적립 금액·주기와 그걸로 잡던 기간별
"매수 예정 → 매수완료" 기록은 리밸런싱과 따로 도는 기능이었고, 쓰는 사람이 "필요 없다"고
정했다. 대신:

| 걷어낸 것 | 대신 |
| --- | --- |
| 종목별 적립 금액·주기(`dca_*`), 매수 기록(`buy_execution`), 매수완료 버튼 | 대시보드의 **마지막 매수 시그널 날짜**. 샀으면 **거래 입력**(수량 더하기 + 평단가 가중평균) |
| 종목별 리밸런싱 주기·리뷰일 | **포트폴리오에 하나** — 분기/반기/연, 직접 정한 날. 리밸런싱은 전체를 한꺼번에 맞추는 일이다 |
| 저장 안 되던 "총 운용자산" 칸 | **현금**(통화별) + 현금 목표비중. 목표비중은 **현금 포함 전체 자금** 기준 |
| — | **평단가** → 평가손익. 비중에는 끼지 않는다 |
| — | **리밸런싱 기록** — 리뷰 때 "지금 상태 기록하기". 남기면 다음 리뷰일이 다음 기간으로 |
| — | 주문 가이드의 **새로 넣을 돈** — 적립하는 달의 답이 여기서 나온다 (저장 안 함) |

마이그레이션 0006은 되돌릴 수 없는 쪽이라(`IRREVERSIBLE`) **백업에 실패하면 시작하지
않는다.** 종목마다 흩어진 리뷰 설정은 사람마다 하나로 모은다 — 가장 많이 쓴 주기(같으면
분기), 직접 정한 날은 *아직 오지 않은 것* 중 가장 이른 날. 지난 날짜를 옮기면 "리뷰 도래"가
영영 켜진 채 남는다.

**리뷰 일정 규칙.** 마감일 E의 리뷰는 `E − 14일` 이후에 남긴 기록이 있으면 끝난 것으로 본다
(하루 일찍 정리했다고 "안 했다"가 되면 안 된다). 지난 기간 리뷰를 건너뛰었으면 새 기간이
시작돼도 "정기 리뷰 도래"가 남지만, **기록을 한 번도 안 남긴 사람에게는 지난 기간을 묻지
않는다** — 기능을 처음 켠 날 할 수 없는 일을 하라는 말이 된다. 달력은 기준통화의 시장을 따른다.

### 2. "관리자 화면 없음"이 뒤집혔다 — 가입 신청 → 승인

허용목록을 `.env`로 두면 사람을 받을 때마다 파일을 고치고 서버를 다시 띄워야 한다. 누구나
로그인은 하되 처음엔 **승인 대기**, 관리자가 사용자 목록에서 **승인**을 누르면 사용자가 되게
한다. 4-4b에 넣는다. 구글 콘솔의 "테스트" 상태(테스트 사용자만 로그인)는 **앱 게시**로 풀어야
하는데, 그건 코드가 아니라 주인이 콘솔에서 한다.

### 3. 문을 열기 전에 막아야 할 구멍이 하나 더 있었다

사용자가 종목 하나 새로고침의 `full=true`(10년치 다시 받기)를 **횟수 제한 없이** 부를 수
있다(`routers/stocks.py`). 6-1에서 "전체 백필은 주인만, 사용자 새로고침은 티커당 쿨다운"으로
정해놓고 아직 안 만들었다. 허용목록에 든 사람만 있는 지금은 괜찮지만 **가입을 열기 전에
반드시** 막는다 — 4-4b. 종목 검색이 외부 조회를 부르는 경우도 같이.

### 남은 순서

| 순서 | 할 일 | 왜 이 자리인가 |
| --- | --- | --- |
| ✅ | 포트폴리오 단순화 + 리밸런싱 기록 (v0.20.0) | 종목 추가 화면이 바뀌므로 사람을 받기 전에. 옮기는 데이터가 주인 것뿐인 지금이 가장 싸다. 분기말(9/30) 전에 기록이 남게 |
| 1 | **4-4b** — 가입 승인, 수집 합집합, `purge`, 상한·쿨다운, 첫 화면 안내, 방침 | 문을 열 준비 |
| 주인 | **규제(3절)·시세 약관(4절) 확인**, 구글 콘솔 **앱 게시** | 둘 다 "4단계 전" 기한이었다. 친구 몇 명일 때는 급하지 않지만 누구나 신청하게 열기 전에는 본다 |
| 2 | **6 푸시 알림** + 뒤로가기로 팝업 닫기 | 관리자에게 가입 신청, 모두에게 매수·비중조절·리뷰 알림. 구독이 사용자별이라 4단계 뒤 |
| 3 | **3b 재무 지표** | 기준 시점·공시 시점 둘 다 저장 (3단계 참고) |
| 4 | **3c AI 분석** | 사용자 본인 키, 브라우저 보관 (3c 상세) |
| 5 | 작은 일들 | 미국 전체 상장목록·검색, 실업률(UNRATE), 지수(S&P500·KOSPI), 6개월 미접속 사용자 종목을 수집에서 뺄지 |
| 선택 | 7 앱스토어 | PWA로 실제로 쓰이는지 본 뒤 |

---

## 3. 규제 — 4단계 전에 확인이 끝나 있어야 한다

**이 문서에서 유일하게 기술이 아닌 항목이고, 계획 자체를 멈출 수 있는 항목이다.**
아래는 법률 자문이 아니라 무엇을 확인해야 하는지에 대한 정리다.

**시점.** 혼자 쓰는 동안(5·6·3단계)은 급하지 않다 — 아래 규제는 전부 *남에게* 무엇을
하느냐를 따진다. 기한은 **4단계 시작 전**이다. 다만 금감원·금융위 문의든 전문가 상담이든
**답이 오는 데 시간이 걸리므로**, 4단계를 꺼내기 직전에 시작하면 거기서 몇 주가 멈춘다.
앞 단계를 진행하는 동안 병행해두는 편이 낫다.

### 두 가지 다른 규제가 있다

- **유사투자자문업** (자본시장법) — *불특정 다수*에게 *대가를 받고* 투자조언.
  핵심 요건에 **대가성**이 있다. 무료면 이 요건이 빠진다.
- **투자자문업** — *개별 고객의 상황에 맞춘* 자문을 *영업으로* 하는 것. 등록제이고
  훨씬 무겁다.

**주의할 점:** 이 계획의 목표인 "**개인화된** 포트폴리오 관리"는 성격상 두 번째 쪽으로
미는 요소다. 반대로 "사용자가 직접 정한 규칙으로 계산해 보여주는 **도구**"라는 성격은
첫 번째에서도 벗어나게 하는 요소다. 실질로 판단되므로 이 균형을 의식하고 만든다.

### 위험을 낮추는 것 (현재 설계가 이미 갖춘 것들)

- **무료** — 유사투자자문의 핵심 요건인 대가성이 없다
- **종목을 추천하지 않는다** — 사용자가 등록한 종목만 다룬다. 우리가 고르지 않는다
- **규칙을 사용자가 정한다** — 목표비중, 밴드, 리뷰 마감일, DCA 금액 전부 사용자 설정
- **자동매매 연동이 없다** — 주문이 나가지 않는다
- **AI는 사용자 본인 키** — 우리가 조언을 생산해 배달하지 않는다

### 위험을 높이는 것 (하지 않을 것)

- 구독료 등 **유료화** → 대가성이 바로 생긴다. 이 선을 넘기 전 반드시 전문가 확인
- "이 종목 사세요" 식 **종목 추천 기능**
- **수익률·성과 광고** ("이 시그널로 연 N%")
- AI가 "매수하세요"라고 **단정하게 만드는 프롬프트**

### 면책 문구만으로는 부족하다

문구는 **필요조건이지 충분조건이 아니다.** 법적 성격은 실질로 판단되므로, 면책 문구를
넣었다고 유료 종목추천이 괜찮아지지 않는다. 반대로 위의 실질 요건이 갖춰져 있으면
문구가 그것을 보강한다. **둘 다 필요하다.**

### 화면·코드에서 정리할 것

- ✅ **"추천"이라는 단어를 걷어냈다** (v0.7.0). `BuyStatus.recommended` → `scheduled`,
  "이번 기간 추천 없음" → "이번 기간 해당일 없음", "추천 금액 합계" → "예정 금액 합계".
  실제 동작은 *"사용자가 설정한 조건이 이 날 충족됐다"*를 기록하는 것이지 우리가 권하는
  게 아니다. 옛 DB 값은 `db._rename_buy_status`가 바꿔준다.
- 첫 진입 시 한 번, 그리고 각 화면에 성격 안내 (지금도 하단에 "참고용" 문구가 있다)
- 이용약관 + 면책조항 (4단계의 개인정보처리방침과 함께)
- AI 프롬프트를 "설명·정리"로 제한 — 예측·단정 금지

### 확인 방법

무료로 유지하는 동안은 위험이 낮은 편이지만 **단정할 수 없다.** 금융감독원·금융위원회에
문의할 수 있고(비조치의견서 제도도 있다), 유료화나 앱스토어 등록 전에는 전문가 상담을
권한다. 관련 법령은 개정이 잦으므로 진행 시점 기준으로 다시 확인한다.

---

## 4. 그 밖의 위험

| 위험 | 대비 |
| --- | --- |
| **시세 출처 약관** — 야후 등은 재배포를 금지한다. 개인용과 다중 사용자 서비스는 성격이 다르다 | **4단계 전** 재검토 — 성격이 갈리는 건 사람이 늘 때다. 공용 캐시가 호출량은 크게 줄여준다. 제공자 계층이 분리돼 있으니 갈아끼울 수 있다 |
| **오라클 ARM 용량 없음** | Docker면 이동 비용이 거의 없다 |
| **AMD 마이크로 1GB RAM** | ARM 확보 실패 시의 문제. 사용자 수에 따라 유료 전환 검토 |
| **구글 OAuth 심사** | 도메인·HTTPS는 5단계, 개인정보처리방침은 4단계 |
| **남의 데이터 노출** | 4단계의 격리 테스트로 강제 |
| **무료 인스턴스 회수 정책** | 매일 갱신이 도니 괜찮을 가능성이 높으나 약관 확인 |
