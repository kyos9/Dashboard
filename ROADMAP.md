# 진행 계획

지금은 한 사람이 자기 PC에서 쓰는 도구다. 목표는 다음과 같다.

> 사람들이 각자 구글 계정으로 들어와, 자기 포트폴리오를 등록하고, 기술적 시그널 +
> 매크로 + 재무 지표를 보고, **자기 AI 키로** 분석을 받고, 리밸런싱 가이드를 받는 도구.
> 폰에 설치해서 앱처럼 쓰고, 서버(오라클 무료 티어 우선)에서 돌린다.

이 문서는 거기까지 가는 순서와, 순서가 그래야 하는 이유를 적는다.

---

## 0. 지금까지 온 자리

어려운 쪽은 이미 지나왔다. Wilder ADX/DMI 직접 구현, 시세 제공자 3개 폴백, 시장별
거래일 캘린더, 환율 환산, 리밸런싱 주문 가이드 — 이건 정답이 없어서 판단을 계속
해야 하는 영역이라 오래 걸린다.

남은 일은 대부분 정답이 정해져 있다. 구글 로그인에는 표준 절차가 있고, Docker도
PWA도 명세대로 하면 된다. **항목 수는 많지만 헤매는 시간은 적다.**

정직하게 크다고 적어둘 것은 셋이다.

1. **사용자 분리** — 이 계획에서 유일한 구조 변경
2. **재무 지표 수집** — 새 데이터 소스는 시세 제공자를 만들 때만큼 든다
3. **규제 확인** — 기술이 아니고, 계획 자체를 멈출 수 있는 유일한 항목

---

## 1. 데이터 경계 — 이 계획에서 가장 가성비 좋은 규칙

코드를 한 줄도 안 쓰고 정하는 규칙인데, 이후 모든 작업의 비용을 좌우한다.

| 공용 (모든 사용자가 나눠 쓴다) | 사용자별 |
| --- | --- |
| 시세 (`price_daily`) | 보는 종목 목록 |
| 지표 (`indicator_daily`) | 보유수량 (`holding`) |
| 시그널 (`signal_daily`) | 매수 기록 (`buy_execution`) |
| 상장목록 (`krx_listing`) | 목표비중·밴드·리뷰 마감일 |
| 환율 **시세** (`usd_krw_rate`) | 기준통화·직접 입력 환율 (`base_currency`, `usd_krw_override`) |
| 매크로 지표, 재무 지표 *(예정)* | AI 키 *(→ 브라우저 보관, 서버에 두지 않는다)* |

**왜 중요한가.** 삼성전자 시세는 사용자가 100명이어도 하루 한 번만 받으면 된다.
지표도 시그널도 사용자와 무관하게 한 번 계산하면 전원에게 쓰인다. 경계를 제대로
그으면 **사용자가 늘어도 서버 부담과 외부 API 호출이 거의 늘지 않는다.** 반대로 대충
그어서 사용자마다 시세를 따로 받으면 비용도 차단 위험도 사람 수에 비례해 커진다.

**새 테이블을 만들 때마다 리비전을 남긴다** (`alembic revision --autogenerate`). 2단계에서
드리프트 검사를 걸어뒀으니 잊으면 테스트가 먼저 잡는다.

**지금 `stocks` 테이블은 두 역할을 겸하고 있다** — "내가 보는 종목"(사용자별)과
"이 티커의 시장·통화"(공용). 4단계에서 `Instrument`(공용) + `UserStock`(사용자별)로
쪼갠다.

### 코드와 대조해보니 — 4단계가 같이 옮겨야 하는 것 넷

분류 자체는 맞았다. `indicators.py`와 `signals.py`는 `Stock`도 설정도 읽지 않으므로,
지표·시그널이 공용이라는 건 코드로 확인된다.

문제는 **표가 적지 않은 것들**이었다. 넷 다 지금은 아무 증상이 없다가 사용자가 둘이
되는 순간 드러나고, 그때는 데이터가 이미 쌓인 뒤라 비싸다. 그래서 지금 적어둔다.

**1. `portfolio_settings`도 `stocks`처럼 두 역할을 겸한다 — 이쪽이 더 나쁘다.**
밴드 기본값·기준통화·직접 입력 환율은 사용자별인데, **환율 캐시(`usd_krw_rate`,
`usd_krw_updated_at`)는 공용**이다. 환율 시세는 모두에게 같기 때문이다. 이대로
사용자별로 쪼개면 100명이 같은 USD/KRW를 100번 따로 받아온다 — `fx.py`의 신선도 판정
(`STALE_AFTER`, 하루)도 사용자마다 따로 돌기 때문이다. **이 표가 막으려던 실패가
정확히 이것이다.** `UserSettings`(사용자별) + 환율 캐시(공용)로 나눈다.

**2. 공용 테이블 다섯 개가 전부 `stocks.ticker`를 외래키로 물고 있다** — `price_daily`,
`indicator_daily`, `signal_daily`, `buy_execution`, `holding`. `stocks`가 사용자별로
쪼개지면 **공용 데이터가 사용자별 테이블을 가리키게 된다.** 실제로 터지는 자리는
`DELETE /api/stocks/{ticker}/purge`다 — 지금은 딸린 행을 전부 지우므로, A가 삼성전자를
완전삭제하면 **B의 시세·지표 히스토리까지 사라진다.** 다섯 개의 외래키를 `Instrument`로
옮긴다.

**3. `Stock.active`가 공용 수집을 켜고 끈다** — 매일 갱신은 `active=True`인 종목만 받는데
(`pipeline.py`), `active`는 "*내* 화면에서 치웠다"는 사용자별 뜻이다. A가 치우면 B의
시세가 갱신을 멈춘다. 수집 대상은 **"누구 하나라도 보고 있는 종목"의 합집합**으로 정한다.

**4. `holding`의 기본키가 `ticker` 하나다** — 사용자별인데 키에 사용자가 없어서, 그대로면
한 티커에 보유수량이 딱 하나다. `(user_id, ticker)`로 간다. `buy_execution`도 중복 판정을
`(ticker, period_start, period_end)`로 하므로 같은 기간에 두 사용자 기록이 충돌한다.

> **앞으로 만드는 모든 테이블은 위 표의 어느 쪽인지 정하고 시작한다.**
> 한 테이블이 양쪽에 걸치면 그 자리에서 쪼갠다 — 미뤘을 때의 값이 위 1번이다.
> 이 규칙이 있어서 3단계(기능 확장)를 4단계(사용자 분리)보다 먼저 해도 안전하다 —
> 새로 붙일 매크로·재무는 둘 다 공용 쪽이고, AI 키는 서버에 두지 않기 때문이다.

---

## 2. 단계

단계 번호는 처음 정한 것을 그대로 둔다(커밋 메시지·버전 기록이 이 번호를 쓴다).
**진행 순서는 번호순이 아니다.**

| 순서 | | 단계 | 크기 | 핵심 |
| --- | --- | --- | --- | --- |
| 완료 | **0** | 기반 정리 ✅ | 작음 | 한 프로세스 · 로그 파일 · 진단 화면 |
| 완료 | **1** | 경계 확정 ✅ | 아주 작음 | 위 표 + 코드 대조 |
| 완료 | **2** | 배포 토대 ✅ | 중간 | Alembic · Postgres 대응 · Docker · 백업 |
| **다음** | **5** | 배포 | 중간 | 오라클 · 도메인 · HTTPS · **비밀번호 잠금** |
| 그다음 | **6** | PWA | 중간 | 설치 · **푸시 알림** · 모바일 다듬기 |
| 이어서 | **3** | 기능 확장 | 큼 | 매크로 → 재무 → AI 분석 |
| **남을 받을 때** | **4** | 사용자 분리 + 로그인 | **가장 큼** | `Instrument`/`UserStock` · 구글 OAuth · 격리 테스트 |
| 선택 | **7** | 앱스토어 | 중간 | Capacitor |

### 왜 이 순서인가

처음에는 3 → 4 → 5 → 6 순이었다. **"5·6단계는 4단계를 요구한다"고 봤는데, 정확히는
*인증*을 요구하지 구글 로그인 + 사용자별 분리를 요구하는 게 아니다.** 혼자 쓰는 동안은
비밀번호 하나로 잠그면 5·6단계가 열린다.

그리고 **혼자 써도 배포에 값이 있다.** 가장 큰 것은 이것이다.

> 지금은 **내 PC를 켜둬야 매일 갱신이 돈다.** 며칠 꺼두면 그 기간 시세가 비고 매수 기간
> 판정도 같이 밀린다. 서버에 있으면 내 PC와 무관하게 돌고, **6단계 푸시 알림도 그래야
> 온다.** "시그널이 떴을 때 알림을 받고 연다"가 이 도구의 실제 사용 방식인데, 그건
> 서버 없이는 성립하지 않는다.

거기에 폰·회사PC 어디서나 열리는 것, 업데이트가 `update.bat` 대신 자동이 되는 것이 붙는다.

4단계를 미루는 데 드는 값은 **없다.** 나중에 할 때 기존 데이터는 전부 "사용자 1번 것"으로
넘기면 되는데, 그게 `Instrument`/`UserStock` 분리가 어차피 하는 일이다. 오히려 1절에서
찾은 넷이 4단계를 예상보다 크게 만들었으므로, 혼자 쓰는 동안(그 넷이 아무 증상도 없는
동안) 미뤄두는 편이 낫다.

3단계가 5·6보다 뒤로 간 이유는 다르다. 3단계는 **구조를 바꾸지 않고 끼워넣는 일**이라
아무 때나 할 수 있고, 배포 뒤에 하면 고칠 때마다 서버에 한 줄로 반영된다.

### 0단계 — 기반 정리 *(완료, v0.7.0)*

- ✅ 프런트를 빌드해 FastAPI가 직접 서빙 (`app/web.py`) → **프로세스 하나, 포트 하나**
  (`http://localhost:8000`). SPA 폴백은 `/api/*`를 삼키지 않고 404를 그대로 낸다.
- ✅ 로그를 파일로 (`backend/logs/app.log`, 5MB × 3 회전). 접속 기록(`uvicorn.access`)은
  일부러 뺐다 — 요청마다 한 줄씩 쌓여 정작 볼 경고를 회전으로 밀어낸다.
- ✅ 진단 화면 (경고·오류만 기본, 트레이스백 포함) + `GET /api/logs/download`
- ✅ `start-all.bat`이 콘솔 없이 뜨고(`scripts/hidden.vbs`), `stop.bat`으로 끈다.
  `update.bat`이 화면까지 다시 빌드한다 — 이걸 빼먹으면 코드를 받아도 옛 화면이 뜬다.

이후 모든 단계의 전제다. 그리고 서버에 올린 뒤 문제가 났을 때 들여다볼 유일한 수단이
로그다. **로그 보기는 반드시 인증 뒤에 둔다** — 종목·에러·내부 경로가 찍히고, 사용자가
여럿이 되면 남의 기록이 섞인다. *(5단계에서 비밀번호로, 4단계에서 계정으로)*

### 2단계 — 배포 토대 *(완료, v0.8.0)*

- ✅ **Alembic 도입** (`backend/migrations/`, `app/migrate.py`). 어려운 건 스키마를 바꾸는
  일이 아니라 **이미 쓰고 있는 DB를 받아들이는 일**이었다. 그 파일에는 테이블이 다 있지만
  Alembic의 기록은 없어서, 그대로 올리면 첫 리비전부터 실행하다 "이미 있다"에서 멈춘다.
  그래서 옛 방식으로 한 번 현행화한 뒤 `0001`을 **도장만 찍고**(stamp) 거기서 이어간다.
  이 경로는 딱 한 번만 일어나고, `db.py`의 옛 마이그레이션 코드는 그 자리에서 끝났다 —
  앞으로의 변경은 전부 리비전으로 간다.
- ✅ **모델·마이그레이션 드리프트 검사** (`test_alembic.py`). 모델에 컬럼을 추가하고
  리비전을 잊으면 **내 PC(테스트는 `create_all`)에서는 멀쩡하고 서버에서만 터진다.**
  가장 늦게, 가장 나쁜 자리에서 발견되는 종류라 자동으로 잡는다.
- ✅ `DATABASE_URL`을 통째로 받는다. 호스팅이 주는 `postgres://` 주소도 그대로 붙여넣을 수
  있다 (SQLAlchemy 2.0은 이 접두사를 거절하므로 내부에서 바꿔준다).
- ✅ Docker + compose. 저장소와 같은 폴더 구조를 이미지 안에서도 유지하고, 남아야 하는
  것은 전부 `/data` 볼륨으로 모았다. 포트는 `127.0.0.1`에만 연다.
- ✅ 백업. **켤 때(최근 12시간 내 없으면) · 매일 · 스키마 바꾸기 직전 · 수동(`backup.bat`)**
  네 갈래로 뜨고 7벌 보관한다. 압축하지 않는다 — 복구가 *파일을 제자리에 갖다 놓기*여야
  급할 때 실제로 복구된다. 돌고 있는 파일을 복사하지 않고 sqlite 백업 API를 쓴다.

**배포는 "완성 후 한 번"이 아니다.** 이 단계를 갖췄으므로 그 뒤로 수정은
`푸시 → 서버에서 한 줄`이 된다. 5단계를 먼저 하는 이유이기도 하다 — 그 뒤의
6·3단계는 만들면서 바로바로 올리는 형태가 된다.

### 3단계 — 기능 확장 *(6단계 뒤에)*

- **3a. 매크로 지표** — 공용 데이터. 갱신 주기가 시세와 다르다(월·분기).
- **3b. 재무 지표** — 공용 데이터. **함정이 있다:** 3분기 실적은 11월에 공시된다.
  이걸 "9월 30일 데이터"로 저장하고 과거를 검증하면 *그 시점에 알 수 없던 정보로
  판단한 셈*이 되어 결과가 실제보다 좋게 나온다. **기준 시점과 공시 시점을 둘 다
  저장해야 한다.** 나중에 고치려면 데이터를 다시 받아야 하므로 만들 때 넣는다.
- **3c. AI 종목분석** — 사용자 개인 API 키.
  - **키는 브라우저에만 두고 서버로 보내지 않는다.** 남의 키를 우리 DB에 저장하는
    순간 책임이 생기고, "암호화 저장"은 복호화 키가 같은 서버에 있어 근본 해결이
    아니다. 서버가 키의 존재 자체를 모르는 편이 가장 안전하다.
  - 제공자별 API 차이는 **시세 제공자와 같은 패턴**으로 추상화한다 (`providers/`).
  - 에러를 번역한다 — 키 오류/잔액 부족/모델 권한 없음은 서로 다른 문제다.
  - **AI는 부가 기능**으로 둔다. 키 발급은 진입 장벽이므로, 키 없이도 나머지 기능은
    전부 동작해야 한다.
  - 프롬프트는 "판단"이 아니라 **"정리·설명"**으로 묶는다 (아래 규제 항목 참고).

### 4단계 — 사용자 분리 + 로그인 *(실제로 남을 받기로 할 때)*

혼자 쓰는 동안은 5단계의 비밀번호 하나로 충분하다. 이 단계는 **두 번째 사람이
들어오는 시점에** 한다.

**구조를 쪼갠다** — 1절 "코드와 대조해보니"에서 찾은 것들이 전부 여기 들어온다.

- `stocks` → `Instrument`(공용) + `UserStock`(사용자별)
- `portfolio_settings` → `UserSettings`(사용자별) + 환율 캐시(공용)
- 공용 테이블 다섯 개(`price_daily`, `indicator_daily`, `signal_daily`, `buy_execution`,
  `holding`)의 외래키를 `Instrument`로 옮긴다. **옮기기 전에는 `purge`가 남의 시세를
  지운다.**
- 매일 갱신 대상을 "누구 하나라도 보고 있는 종목"의 합집합으로
- `holding`의 기본키를 `(user_id, ticker)`로. `buy_execution`의 중복 판정에도
  사용자를 넣는다

**로그인을 붙인다.**

- 구글 OAuth (비밀번호를 보관하지 않아도 된다)
- **개인정보처리방침** — 남의 계정 정보를 받는 순간 필요하고, 구글 OAuth 심사도 요구한다
- **모든 쿼리에 사용자 스코프.** 한 곳만 빠뜨려도 남의 데이터가 그대로 노출된다.
  눈으로 막을 수 있는 종류가 아니므로 **"A로 로그인해 B의 데이터가 안 보이는지"
  검사하는 테스트**를 따로 둔다.
- 로그·진단 화면의 잠금을 비밀번호에서 계정으로 바꾼다

### 5단계 — 배포

- 오라클 무료 티어: ARM(Ampere A1, 4코어/24GB)이 좋지만 **인기 리전에서 "용량 없음"이
  잦다.** 항상 잡히는 AMD 마이크로는 RAM 1GB라 pandas·numpy에 빠듯하다. Docker면 다른
  곳으로 옮기는 비용이 거의 없으니 막히면 갈아탄다.
- 도메인 + HTTPS
- **비밀번호 하나로 잠근다.** 공개된 주소에 그냥 올리면 **아무나 진단 화면과 로그를
  본다** — 종목·에러·내부 경로가 전부 찍힌다. 혼자 쓰는 동안 필요한 건 "아무나 못
  들어온다"이지 사용자별 분리가 아니므로, 여기서는 잠그기만 하고 계정은 4단계로 미룬다.
  (개인정보처리방침도 남의 계정 정보를 받기 시작하는 4단계로 함께 간다.)
- **스케줄러 중복 실행 방지** — APScheduler가 앱 안에서 돈다. 워커를 여럿 띄우면 같은
  갱신이 여러 번 실행된다. 단일 스케줄러 프로세스 또는 DB 락.
- 백업 자동화

### 6단계 — PWA

- manifest, 아이콘, 서비스워커, 설치 유도
- 오프라인·로딩 화면 (흰 화면 번쩍임 없애기)
- **푸시 알림 — 이 단계의 핵심.** 부록이 아니다. 이 도구의 성격상 매일 대시보드를
  열어보는 사람은 적고, *"시그널이 떴습니다" 알림이 오면 여는 것*이 실제 사용 방식이
  된다. 있고 없고가 "가끔 여는 사이트"와 "쓰는 앱"을 가른다.
- 모바일 터치 타겟·뒤로가기 동작 다듬기

"앱처럼 보이는 완성도"는 배포 형태가 아니라 여기서 온다. exe로 감싼다고 해결되지
않고, 이게 잘 되어 있으면 대부분의 사용자는 PWA와 앱을 구분하지 못한다.

### 7단계 — 선택: 앱스토어

Capacitor로 **지금 React 코드를 그대로** 네이티브 껍데기에 넣는다. 서버는 어차피
있으니 버려지는 작업이 없다.

비용: Apple 연 $99 / Google 1회 $25, 심사 며칠, 그리고 **투자 관련 앱은 심사가 까다로운
편이다** (아래 규제 항목이 서류로 돌아온다). PWA로 먼저 실제로 쓰이는지 본 다음 결정한다.

> **PyInstaller exe / Electron은 하지 않는다.** 백엔드를 사용자 PC에 설치하는 방향이라,
> 서버에 올리는 순간 그 작업이 쓸모없어진다. 앱 경험은 PWA → Capacitor로 간다.

---

## 2-1. 중간점검에서 찾은 것 (v0.8.1)

5단계로 넘어가기 전에 코드를 한 번 훑었다. 여섯 개가 나왔고 **셋은 고쳤다.**

고친 것 — 셋 다 같은 이야기였다. *개인 PC는 꺼진다.*

- ✅ **켤 때 도는 작업이 9시간 뒤에 잡히고 있었다.** 스케줄러는 UTC로 도는데 시간대
  없는 `datetime.now()`를 줘서, APScheduler가 그걸 UTC로 읽었다. 한국(UTC+9)에서는
  "15초 뒤"가 9시간 뒤가 된다 — **켤 때 하는 백업과 상장목록 갱신이 한 번도 안 돌고
  있었다.** 만들어둔 기능이 조용히 죽어 있는 종류라, 로그에도 아무 흔적이 없었다.
- ✅ **갱신이 하루라도 밀리면 그 사이 시그널이 영영 기록되지 않았다.** 매수 판정이
  "마지막 시그널 날짜" 하루만 봤다. 이제 기간 전체에서 **처음** 뜬 날을 찾는다
  (등록일 이전으로는 가지 않는다 — 그때는 알 수도 없던 일이다).
- ✅ **켤 때 시세를 따라잡는 장치가 없었다.** cron은 놓친 실행을 되돌려주지 않는다.
  백업에는 이 장치가 있었는데(`backup_startup`) 시세에는 없어서, 며칠 꺼뒀다 켜면
  시세가 그대로였다. 뒤처진 **시장만** 골라 받아온다.

> 앞의 둘이 겹치면 이렇게 된다: PC를 며칠 꺼둔다 → 그동안 갱신이 안 돈다 → 켜서
> 새로고침해도 마지막 날만 판정된다 → **그 사이 매수 시그널이 통째로 사라진다.**
> 서버에 올리면 늘 켜져 있어 증상이 가려지므로, 배포 전에 고치는 게 맞았다.

남은 셋 — 지금 고장난 건 아니지만 미루면 비싸진다.

- ⬜ **`SIGNAL_APP_SPEC.md`가 저장소에 없다.** 코드 6곳과 README가 참조하는데 파일이
  없고 git 이력에도 없다. 지표·시그널 공식의 근거가 사라진 상태다. 지금 구현을 그대로
  문서로 복원해 넣으면 된다.
- ⬜ **리밸런싱 이력이 남지 않는다.** 전부 실시간 계산이고 `Holding.quantity`는
  덮어쓰기라 과거 비중을 복원할 수 없다. 분기마다 리뷰하는 도구인데 지난 리뷰를 볼 수
  없다. **나중에 넣어도 그 전 기록은 못 살린다** — 3b 재무의 공시 시점 함정과 같다.
- ⬜ **Postgres를 한 번도 안 돌려봤다.** CI는 SQLite만 돌고 `conftest`의 세션도
  `sqlite:///:memory:` 고정이다. `0001_initial.py`가 진짜 Postgres에 적용된 적이 없다.
  **5단계가 바로 이걸 쓴다** — CI에 Postgres 서비스를 붙여 같은 테스트를 한 번 더
  돌리는 것으로 막는다.

---

## 3. 규제 — 4단계 전에 확인이 끝나 있어야 한다

**이 문서에서 유일하게 기술이 아닌 항목이고, 계획 자체를 멈출 수 있는 항목이다.**
아래는 법률 자문이 아니라 무엇을 확인해야 하는지에 대한 정리다.

**시점.** 혼자 쓰는 동안(5·6·3단계)은 급하지 않다 — 아래 규제는 전부 *남에게* 무엇을
하느냐를 따진다. 기한은 **4단계 시작 전**이다. 다만 금감원·금융위 문의든 전문가 상담이든
**답이 오는 데 시간이 걸리므로**, 4단계를 꺼내기 직전에 시작하면 거기서 몇 주가 멈춘다.
앞 단계를 진행하는 동안 병행해두는 편이 낫다.

### 두 가지 다른 규제가 있다

- **유사투자자문업** (자본시장법) — *불특정 다수*에게 *대가를 받고* 투자조언.
  핵심 요건에 **대가성**이 있다. 무료면 이 요건이 빠진다.
- **투자자문업** — *개별 고객의 상황에 맞춘* 자문을 *영업으로* 하는 것. 등록제이고
  훨씬 무겁다.

**주의할 점:** 이 계획의 목표인 "**개인화된** 포트폴리오 관리"는 성격상 두 번째 쪽으로
미는 요소다. 반대로 "사용자가 직접 정한 규칙으로 계산해 보여주는 **도구**"라는 성격은
첫 번째에서도 벗어나게 하는 요소다. 실질로 판단되므로 이 균형을 의식하고 만든다.

### 위험을 낮추는 것 (현재 설계가 이미 갖춘 것들)

- **무료** — 유사투자자문의 핵심 요건인 대가성이 없다
- **종목을 추천하지 않는다** — 사용자가 등록한 종목만 다룬다. 우리가 고르지 않는다
- **규칙을 사용자가 정한다** — 목표비중, 밴드, 리뷰 마감일, DCA 금액 전부 사용자 설정
- **자동매매 연동이 없다** — 주문이 나가지 않는다
- **AI는 사용자 본인 키** — 우리가 조언을 생산해 배달하지 않는다

### 위험을 높이는 것 (하지 않을 것)

- 구독료 등 **유료화** → 대가성이 바로 생긴다. 이 선을 넘기 전 반드시 전문가 확인
- "이 종목 사세요" 식 **종목 추천 기능**
- **수익률·성과 광고** ("이 시그널로 연 N%")
- AI가 "매수하세요"라고 **단정하게 만드는 프롬프트**

### 면책 문구만으로는 부족하다

문구는 **필요조건이지 충분조건이 아니다.** 법적 성격은 실질로 판단되므로, 면책 문구를
넣었다고 유료 종목추천이 괜찮아지지 않는다. 반대로 위의 실질 요건이 갖춰져 있으면
문구가 그것을 보강한다. **둘 다 필요하다.**

### 화면·코드에서 정리할 것

- ✅ **"추천"이라는 단어를 걷어냈다** (v0.7.0). `BuyStatus.recommended` → `scheduled`,
  "이번 기간 추천 없음" → "이번 기간 해당일 없음", "추천 금액 합계" → "예정 금액 합계".
  실제 동작은 *"사용자가 설정한 조건이 이 날 충족됐다"*를 기록하는 것이지 우리가 권하는
  게 아니다. 옛 DB 값은 `db._rename_buy_status`가 바꿔준다.
- 첫 진입 시 한 번, 그리고 각 화면에 성격 안내 (지금도 하단에 "참고용" 문구가 있다)
- 이용약관 + 면책조항 (4단계의 개인정보처리방침과 함께)
- AI 프롬프트를 "설명·정리"로 제한 — 예측·단정 금지

### 확인 방법

무료로 유지하는 동안은 위험이 낮은 편이지만 **단정할 수 없다.** 금융감독원·금융위원회에
문의할 수 있고(비조치의견서 제도도 있다), 유료화나 앱스토어 등록 전에는 전문가 상담을
권한다. 관련 법령은 개정이 잦으므로 진행 시점 기준으로 다시 확인한다.

---

## 4. 그 밖의 위험

| 위험 | 대비 |
| --- | --- |
| **시세 출처 약관** — 야후 등은 재배포를 금지한다. 개인용과 다중 사용자 서비스는 성격이 다르다 | **4단계 전** 재검토 — 성격이 갈리는 건 사람이 늘 때다. 공용 캐시가 호출량은 크게 줄여준다. 제공자 계층이 분리돼 있으니 갈아끼울 수 있다 |
| **오라클 ARM 용량 없음** | Docker면 이동 비용이 거의 없다 |
| **AMD 마이크로 1GB RAM** | ARM 확보 실패 시의 문제. 사용자 수에 따라 유료 전환 검토 |
| **구글 OAuth 심사** | 도메인·HTTPS는 5단계, 개인정보처리방침은 4단계 |
| **남의 데이터 노출** | 4단계의 격리 테스트로 강제 |
| **무료 인스턴스 회수 정책** | 매일 갱신이 도니 괜찮을 가능성이 높으나 약관 확인 |
