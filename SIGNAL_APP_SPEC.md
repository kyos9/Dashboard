# 신호판 시그널 명세

이 문서는 **무릎매수(v2)** 와 **어깨매도(참고)** 시그널, 그리고 그것을 둘러싼 매수·리밸런싱
워크플로우의 정의다. 코드가 무엇을 하는지가 아니라 **무엇을 해야 하는지**를 적는다.

---

## 1. 이 문서에 대하여 — 먼저 읽을 것

**이 문서는 구현된 코드에서 복원한 것이다.** 원본은 이 저장소에 커밋된 적이 없고
(git 이력에도 없다) 코드 여섯 곳과 README가 참조만 하고 있었다. 2026-09-17 중간점검에서
발견해 되살렸다.

그래서 **복원된 것과 복원되지 않은 것을 구분해 둔다.**

| | |
| --- | --- |
| ✅ 복원됨 | 지표 공식, 시그널 조건식, 임계값, 워크플로우 규칙 — 전부 코드에서 1:1로 읽어냈다 |
| ❌ 복원 못 함 | **백테스트 결과**(원본에는 investing.com 종가 기준 검증이 있었다), **임계값을 그 값으로 정한 근거** |

두 번째 줄이 중요하다. *"ADX 20이 왜 20인가"* 에 대한 답은 이 문서에 없다. 원본에 있었을
것이고, 지금은 없다. **그러므로 이 문서는 "지금 이렇게 동작한다"의 근거이지, "이 값이
옳다"의 근거가 아니다.** 임계값을 바꾸려 할 때 이 차이가 중요해진다.

> **이 문서와 코드가 어긋나면 그건 버그다.** 어느 쪽이 맞는지 판단한 뒤 둘 중 하나를
> 고친다. 조용히 한쪽만 두지 않는다.

---

## 2. 지표

모든 지표는 **일봉**에서 계산한다. 입력은 날짜 오름차순으로 정렬된
`open / high / low / close / volume`이다.

### 2.1 종가 기준 — 이게 어긋나면 나머지가 전부 어긋난다

**분할(split)은 반영하고 배당 재투자는 반영하지 않은 종가**를 쓴다.
(yfinance 기준 `auto_adjust=False`로 받은 `Close`.)

원본 백테스트가 investing.com 종가 기준이었기 때문이다. 배당조정 종가를 쓰면 이동평균·
이격도·표준편차가 전부 미묘하게 어긋난다. `Adj Close`는 참고용으로 저장만 하고
**지표 계산에는 쓰지 않는다.**

### 2.2 이동평균·변동성·거래량

| 지표 | 정의 |
| --- | --- |
| `ma5` `ma20` `ma50` `ma200` | 종가 단순이동평균 |
| `stddev20` | 종가 20일 표준편차, **모집단 기준**(`ddof=0`) — 20거래일 구간 자체를 모집단으로 본다 |
| `vol_ma5` `vol_ma20` | 거래량 단순이동평균 |
| `vol_ratio` | `vol_ma5 / vol_ma20` |
| `roc5` | `(종가 / 5일전 종가 − 1) × 100` |
| `disparity` (이격도) | `(종가 / ma20 − 1) × 100` — **기준은 MA20 고정** |

`ma5`, `ma200`, `roc5`는 **시그널 판정에 쓰이지 않는다.** 화면에 보여주기 위한 값이다.
시그널이 실제로 쓰는 것은 `ma20`(이격도), `ma50`, `stddev20`, `vol_ratio`,
`vol_ma5`/`vol_ma20`, `plus_di`/`minus_di`/`adx`뿐이다.

### 2.3 +DI / −DI / ADX — Wilder 14일

외부 TA 라이브러리에 기대지 않고 직접 구현한다. 라이브러리마다 스무딩 방식이 달라
같은 이름의 지표가 다른 값을 내기 때문이다.

기간은 **14일**.

```
UpMove   = high − 이전 high
DownMove = 이전 low − low

+DM = UpMove   (UpMove > DownMove 이고 UpMove > 0 일 때), 아니면 0
−DM = DownMove (DownMove > UpMove 이고 DownMove > 0 일 때), 아니면 0

TR = max(high − low, |high − 이전 종가|, |low − 이전 종가|)
```

세 값(`TR`, `+DM`, `−DM`)에 **Wilder 스무딩(RMA)** 을 적용한다.

```
첫 값   = 유효한 첫 14개의 단순평균
이후    = (직전값 × 13 + 당일값) / 14
```

```
+DI = 100 × 스무딩(+DM) / 스무딩(TR)
−DI = 100 × 스무딩(−DM) / 스무딩(TR)
DX  = 100 × |+DI − −DI| / (+DI + −DI)
ADX = Wilder 스무딩(DX)
```

**첫 행의 `TR`/`+DM`/`−DM`은 NaN으로 둔다.** 전일 종가가 없으므로 값이 없는 게 맞고,
0으로 채우면 워밍업 구간의 스무딩이 전부 틀어진다.

### 2.4 워밍업

`MA200`과 ADX를 제대로 채우려면 과거 데이터가 충분해야 한다. **항상 최소 260거래일**을
유지한다. 그 전 구간의 지표는 NaN이며, NaN인 값은 조건 판정에서 "충족 안 됨"으로
떨어진다 (3장 참고).

---

## 3. 무릎매수 (v2)

> 떨어지는 중이되, 떨어지는 속도가 잦아들거나 거래가 붙기 시작한 자리.

네 조건을 **모두** 만족한 날에 발동한다.

| | 조건 | 뜻 |
| --- | --- | --- |
| 1 | `−DI > +DI` | 하락 방향이 우세하다 |
| 2 | `이격도 < 0` | 종가가 MA20 아래에 있다 |
| 3 | `stddev20 < 5거래일 전 stddev20` **또는** `vol_ratio > 1.1` | 변동성이 줄고 있거나, 거래량이 붙고 있다 |
| 4 | `ADX > 20` | 추세라고 부를 만한 움직임이 있다 (횡보 구간 제외) |

```
knee_buy_v2 = (−DI > +DI)
            AND (이격도 < 0)
            AND (stddev20 < stddev20[5일 전] OR vol_ratio > 1.1)
            AND (ADX > 20)
```

**경계값 처리.** 부등호는 전부 **엄격**하다. 같으면 충족되지 않는다.

- `ADX == 20` → 조건 4 **불충족**
- `이격도 == 0` → 조건 2 **불충족**
- `vol_ratio == 1.1` → 조건 3의 뒷항 **불충족**
- `stddev20 == 5일 전 값` → 조건 3의 앞항 **불충족**

**값이 없으면(NaN) 불충족이다.** 워밍업 구간이나 데이터 결측일에 조건이 참으로
새어 나가지 않게 하려는 것이다. 최종 결과에 `NaN → False`를 적용한다.

조건 3은 두 개가 OR이므로, 화면에서는 네 칸이 아니라 **"변동성 또는 거래량"** 한 칸으로
묶어 보여준다. 둘 다 판정 불가면 그 칸이 판정 불가가 된다.

---

## 4. 어깨매도 (참고)

> 올라가 있고, 추세가 강해지고 있고, 거래가 받쳐주는 자리.

**이름 그대로 참고용이다. 이 신호는 매도를 실행시키지 않는다.** 매도 실행일은 6장의
고정 리뷰 마감일이며, 어깨 신호는 그 기간에 이런 자리가 있었다는 표시로만 쓰인다.

| | 조건 |
| --- | --- |
| 1 | `종가 > MA50` |
| 2 | `ADX > 5거래일 전 ADX` |
| 3 | `vol_ma5 > vol_ma20` |

```
shoulder_sell_ref = (종가 > MA50) AND (ADX > ADX[5일 전]) AND (vol_ma5 > vol_ma20)
```

경계값·NaN 처리는 3장과 같다.

---

## 5. 매수 실행 워크플로우

### 5.1 기간

사용자가 종목마다 **매수 주기**(월 / 분기)와 **금액**을 정한다. 기간의 경계는 달력이
아니라 **그 종목이 속한 시장의 거래일 캘린더**로 정한다 (미국 NYSE / 한국 XKRX).

종목마다 결측일이 달라서 개별 종목의 데이터로 경계를 잡으면 같은 달인데 종목마다
기간이 달라진다. 그리고 한국과 미국은 휴장일이 달라서, 국내 종목에 NYSE 캘린더를 쓰면
분기 마지막 거래일이 실제로 장이 열리지 않는 날로 잡힌다.

### 5.2 판정

한 기간에 대해 **기록은 하나만** 만든다.

1. 기간 안에서 `knee_buy_v2`가 **처음** 뜬 날 → 그날로 **시그널 매수** 기록
2. 기간 마지막 거래일까지 한 번도 안 떴으면 → 마지막 거래일에 **폴백 매수** 기록

> **"처음 뜬 날"이지 "마지막으로 확인한 날"이 아니다.** 갱신은 매일 돈다는 보장이
> 없다(개인 PC는 꺼진다). 마지막 날만 보면, 갱신이 밀린 사이에 뜬 시그널이 영영
> 기록되지 않는다.

**탐색 범위는 `기간 시작일`과 `종목 등록일` 중 늦은 쪽부터**다. 종목을 새로 넣으면 전체
히스토리가 백필되면서 등록 전 시그널도 함께 들어오는데, 그걸 매수로 잡으면 *그때는 알
수도 없었고 실제로 사지도 않은 거래*가 예정으로 올라온다.

### 5.3 예정 → 확정

기록은 **예정(`scheduled`)** 으로 생긴다. 이건 권유가 아니라 *"사용자가 설정한 조건이 이
날 충족됐다"* 는 기록이다.

사용자가 화면에서 **매수완료**를 눌러야 **확정(`confirmed`)** 이 된다. 확정할 때
보유수량에 자동 반영할지 고를 수 있고, 반영하면 `금액 ÷ 그날 종가`만큼 더한다
(수동 관리와의 이중 입력을 막기 위해서다).

---

## 6. 리밸런싱과 비중조절 신호

### 6.1 비중 계산 — 통화를 먼저 맞춘다

원화 종목과 달러 종목의 평가금액을 그냥 더하면 비중이 완전히 틀어진다. **모든 평가금액을
기준통화로 환산한 뒤** 비중을 계산한다.

```
평가금액      = 보유수량 × 최신 종가            (그 종목의 거래 통화)
평가금액(기준) = 환산(평가금액 → 기준통화)
실제비중(%)   = 평가금액(기준) / Σ평가금액(기준) × 100
초과분(%p)    = 실제비중 − 목표비중
```

주문 금액은 반대로 **실제 거래하는 통화**로 보여준다. 주수를 구할 때도 현지 종가로
나눈다. 그래서 양쪽을 모두 들고 다닌다.

### 6.2 리뷰 마감일

- 사용자가 종목별로 직접 지정한 날짜가 있으면 **그 값을 쓴다** (세금정산일 등)
- 없으면 리밸런싱 주기(분기 / 반기)의 **마지막 거래일**을 시장 캘린더로 계산한다

### 6.3 비중조절 신호

```
비중조절 신호 = (오늘 ≥ 리뷰 마감일)
              OR (초과분 > 밴드)
              OR (초과분 < −밴드)
```

밴드는 종목별로 정할 수 있고, 비워두면 전역 기본값을 쓴다.

사유는 겹칠 수 있으며 각각 표시한다.

| 사유 | 뜻 |
| --- | --- |
| 정기 리뷰 도래 | 마감일이 됐다 |
| 밴드 초과 | 과중비중 → **매도 검토** |
| 밴드 미달 | 저비중 → **매수 검토** |

> **이 신호는 자동 매매를 트리거하지 않는다.** "지금 확인해볼 때가 됐다"는 조기 알림일
> 뿐이고, **매도 실행일 자체는 6.2의 고정 리뷰 마감일**이다.

해당 리뷰 기간 안에 어깨매도(참고)가 떴는지도 함께 표시한다.

### 6.4 주문 가이드

목표비중과 실제비중의 차이를 금액과 주수로 환산해 보여준다. 총 운용자산은 보유 평가금액
합계를 쓰되, 사용자가 직접 입력하면 그 값을 쓴다 (미투자 현금을 포함시키기 위해서다).

**조정 필요금액이 총자산의 0.5% 미만이면 "유지"로 본다.** 거래비용을 생각하면 그만한
금액을 맞추려고 주문을 내는 건 손해다.

---

## 7. 화면

| 화면 | 보여주는 것 |
| --- | --- |
| **대시보드** | 종목별 최신 지표, 무릎 조건 충족 여부, 이번 기간 매수 예정(+ 매수완료 버튼), 어깨매도 참고, 비중조절 신호 |
| **종목 관리** | 종목 추가·삭제, 매수 금액/주기, 목표비중, 밴드, 리뷰 마감일 |
| **히스토리 차트** | 로그스케일 종가 + **시그널이 뜬 날** 표시 |
| **리밸런싱** | 보유수량, 목표 대비 실제비중, 리뷰 마감일, 주문 가이드 |

**차트 표시와 매수 기록은 다른 것을 본다.** 차트는 시그널 판정을 그대로 찍으므로 백필된
전 구간이 나오고, 매수 기록은 5.2의 등록일 제한을 받는다. 차트는 *정보*이고 매수 기록은
*할 일*이라 기준이 다르다.

---

## 8. 다루지 않는 것

- 자동 매매·브로커 연동 (**주문이 나가지 않는다**)
- 종목 추천 — 사용자가 등록한 종목만 다룬다
- 손절 규칙, 레버리지, 파생
- 분봉·틱 데이터 (일봉만)
- 세금·수수료 계산

---

## 9. 가정과 기준

| | |
| --- | --- |
| 이격도 기준 | **MA20 고정** |
| ADX 기간 | **14일**, Wilder 스무딩 |
| 표준편차 | 모집단(`ddof=0`) |
| "5일 전" | 달력 5일이 아니라 **5거래일 전 행** |
| "오늘" | **각 시장 현지 시각 기준.** 서버 시계로 재면 한쪽이 하루 더 오래돼 보인다 |
| 거래일 캘린더 | 미국 NYSE / 한국 XKRX |
| 값이 없을 때 | 조건 불충족 (참으로 새어 나가지 않게) |
| 기간 경계 | 개별 종목 데이터가 아니라 시장 캘린더 |

---

## 10. 이 명세를 바꿀 때

시그널 정의를 바꾸면 **과거 판정이 함께 바뀐다.** 지표와 시그널은 저장된 시세에서 다시
계산되므로, 조건식을 고치고 재계산하면 작년 차트의 표시도 달라진다. 그게 대개 원하는
동작이지만, 이미 **확정된 매수 기록(`confirmed`)은 실제로 있었던 일**이라 다시 계산해서
지우면 안 된다.

바꾸는 순서:

1. 이 문서를 먼저 고친다
2. `app/services/signals.py`(또는 `indicators.py`)를 고친다
3. 경계값 테스트를 고치거나 더한다 (`tests/test_signals.py`, `tests/test_indicators.py`)
4. 전 종목 재계산
