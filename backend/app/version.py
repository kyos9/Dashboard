"""실행 중인 코드가 어느 버전인지 알려준다.

`git pull`을 했는지 / 서버를 다시 켰는지 화면에서 바로 확인할 수 있어야 한다.
이게 없으면 "고쳤는데 그대로예요"가 코드 문제인지 옛날 코드가 도는 건지 구분이 안 된다.
"""

import datetime as dt
import os
import subprocess
from functools import lru_cache
from pathlib import Path

# 배포 토대를 갖춘 버전 — Alembic으로 스키마를 관리하고, DATABASE_URL로 Postgres까지
# 받고, 백업이 자동으로 뜨고, Docker로 어디서든 같은 모양으로 뜬다.
# 0.8.1: PC가 꺼져 있던 동안을 제대로 다루게 고쳤다 (중간점검에서 찾은 셋).
# 0.9.0: 바깥에 내놓을 수 있게 됐다 — 비밀번호 잠금, 스케줄러 단일 실행, HTTPS 구성.
# 0.9.1: 한국어 윈도우에서 앱이 안 뜨던 것을 고쳤다 (alembic.ini를 cp949로 읽던 문제).
# 0.10.0: 일본주식(도쿄) 추가. 환율을 달러 하나에서 통화별로 일반화했다.
# 0.10.1: 종목 관리에서도 삭제, 삭제 확인을 화면 가운데 팝업으로, 순서 이동을 눈에 보이게.
# 0.11.0: 폰에 설치해 앱처럼 쓸 수 있게 됐다 — 아이콘·manifest·서비스 워커(오프라인),
#         설치 버튼, 노치 회피와 손가락 크기 다듬기. 푸시 알림은 아직이다.
# 0.12.0: 매크로 탭이 생겼다 — 지표 카드·장단기 금리차·지표별 차트, 그리고 공포·탐욕
#         지수(CNN). 여기 숫자는 매수·매도 판정에 들어가지 않는다.
# 0.12.1: 정기 갱신이 건너뛰어져 매크로 날짜가 하루 뒤처지던 것을 고쳤다. 카드에
#         "언제 받아봤는지"를 같이 띄운다.
# 0.12.2: 공포·탐욕 지수가 안 들어오던 것을 고쳤다 — 이 지수에 없는 20년 전을 묻고
#         있었다. 실패 안내가 엉뚱한 출처 이름을 대던 것도 같이 고쳤다.
# 0.13.0: 국면 배지가 생겼다 — 금리 역전·공포 구간·공포·탐욕 극단이 매크로 탭 맨 위에
#         뜬다(합성 점수는 만들지 않는다). 공포·탐욕 지수는 어느 구간인지 글자로 적고,
#         ☆ 로 고른 지표는 홈 화면 맨 위에 한 줄로 뜬다.
# 0.13.1: 국면 배지를 홈에도 띄운다. 홈 기본 지표를 VIX · 장단기 금리차 · 공포·탐욕으로
#         바꿨다 (셋 다 매일 움직이고, 셋 다 배지가 보는 값이다). 금리차도 ☆ 로 켜고 끈다.
# 0.14.0: 물가 예상치를 카드에서 직접 넣는다. 발표가 예상을 넘으면 "물가 상회" 배지가
#         뜬다 — 비교할 예상치가 없으면 아무 말도 안 한다(우리가 정한 기준은 안 쓴다).
# 0.15.0: 물가 예상치를 클리블랜드 연준 나우캐스트에서 자동으로 받는다. 저쪽은 전월비를
#         내므로 전년비로 바꿔 넣는다 — 그대로 넣으면 매달 "물가 상회"가 뜬다.
# 0.15.1: 물가 예상치 칸에 마이너스를 칠 수 없던 것을 고쳤다 — 빼기표를 친 직후 커서가
#         그 앞으로 밀려서, 다음 글자가 "0-" 을 만들고 빼기표가 조용히 사라졌다.
# 0.16.0: 해외 종목·ETF를 한글로 찾는다 — "애플", "나스닥100", "미국채20년". 야후는 미국
#         이름을 영문으로만 주므로 자주 보는 139종목에 한글 별칭을 붙여 넣었다. 그리고
#         "apple" 이 `APPLE` 이라는 없는 티커가 되던 것을 고쳤다 — 영문 열 자 이하는
#         전부 티커로 보여서, 이름 검색까지 가지도 못하고 빈 종목이 등록됐다.
# 0.17.0: 표를 사용자별로 쪼갰다 (4-1, 마이그레이션 0005) — 종목은 공용(instrument)과
#         내 것(user_stock)으로, 보유·매수기록·설정에는 사용자가 붙었다. 로그인은 아직
#         없고 쓰던 데이터는 전부 1번 사용자 것이라 화면은 그대로다. 되돌릴 수 없는
#         변경이라 직전 백업에 실패하면 업그레이드를 시작하지 않는다.
# 0.17.1: 모든 조회가 요청한 사람 것만 본다 (4-2). 화면은 그대로다 — 아직 사람이 한
#         명이다. 한 가지만 달라졌다: 환율을 직접 넣은 통화도 자동 환율을 받아둔다
#         (받은 값은 모두가 같이 쓰고, 직접 넣은 값은 그 사람 화면에만 얹힌다).
# 0.18.0: 구글 계정으로 로그인한다 (4-3). 켜면 비밀번호 문은 닫히고 계정마다 자기 종목·
#         포트폴리오가 따로 보인다. 주인 계정은 지금 데이터(1번)에 붙고, 허용목록에 있는
#         사람만 들어오며, 주인이 아니면 탈퇴할 수 있다. 안 켜면 예전 그대로다.
# 0.19.0: 로그인 전에도 들어와 둘러본다 (4-4). 구글 모드에서 손님은 매크로를 보고,
#         내 종목이 필요한 화면에서 로그인을 권한다. 관리자(주인)와 사용자를 나눴다 —
#         전체 새로고침·진단·예상치 입력·매크로/환율/상장목록 받아오기는 관리자만.
# 기능이 바뀔 때마다 올린다.
# 0.20.0: 포트폴리오를 단순하게 — 종목마다 적던 적립 금액·주기와 매수 기록(예정·확정)을
#         걷어내고, 종목은 수량·평단가·목표비중만 받는다. 목표비중은 현금까지 포함한 전체
#         자금 기준이고, 리뷰 주기는 포트폴리오에 하나다. 리뷰 때 "지금 상태 기록하기"로
#         리밸런싱 기록을 남긴다 (마이그레이션 0006, 백업 필수).
# 0.20.1: 느리던 곳을 고쳤다 — 대시보드가 종목마다 수십 년치를 다 줄 세운 뒤 최근 몇 줄만
#         쓰고 있었다(최근 몇 주만 보게 해서 Postgres 기준 465→32ms). 남이 이미 담은 종목을
#         등록할 때 전체 기간을 다시 받지 않는다. 서버를 켤 때 거래일 캘린더를 미리 만들어
#         업데이트 직후 첫 화면이 기다리지 않는다. 차트 코드는 차트를 열 때 받는다.
APP_VERSION = "0.20.1"

REPO_ROOT = Path(__file__).resolve().parents[2]


# 이미지를 만들 때 박아 넣는 커밋 해시 (Dockerfile 의 ARG / .github/workflows/docker.yml).
#
# **도커에는 `.git` 이 없다**(.dockerignore). 그래서 아래 `git rev-parse` 가 서버에서는
# 항상 빈손으로 돌아오고, 화면에는 `0.11.0` 만 남는다. 그 상태로는 서버가 오늘 이미지를
# 받았는지 어제 것을 그대로 쓰고 있는지 알 방법이 없다 — 이 모듈이 애초에 있는 이유가
# 그걸 알자는 것이었는데 정작 서버에서 안 되고 있었다.
REVISION_ENV = "APP_REVISION"

# 언제 만든 이미지인가 (ISO8601 UTC). 역시 빌드할 때 박는다.
#
# 해시만으로는 **사람이 못 읽는다.** `e81f1ee` 는 순서도 뜻도 없는 지문이라, 화면에서
# 그것만 보고는 지금 도는 것이 어제 것인지 방금 받은 것인지 알 수 없다. 대조는 되지만
# 그건 만든 사람의 용도이고, 화면은 쓰는 사람이 보는 자리다. 날짜를 같이 준다.
#
# 문자열 그대로 내보내고 **화면에서 읽기 좋게 바꾼다.** 서버에서 "9월 21일"로 만들어
# 보내면 서버의 시간대(UTC)로 찍히는데, 보는 사람은 다른 시간대에 있다.
BUILT_AT_ENV = "APP_BUILT_AT"


@lru_cache(maxsize=1)
def git_revision() -> str | None:
    """현재 실행 중인 코드의 커밋. 알 수 없으면 None.

    빌드할 때 박아둔 값을 먼저 본다 (도커). 없으면 저장소에 직접 물어본다 (개인 PC).
    """
    baked = os.environ.get(REVISION_ENV, "").strip()
    if baked:
        # 워크플로가 전체 해시를 넘긴다. 화면에는 앞 7자리면 충분하다.
        return baked[:7]
    try:
        result = subprocess.run(
            ["git", "-C", str(REPO_ROOT), "rev-parse", "--short", "HEAD"],
            capture_output=True,
            text=True,
            timeout=5,
        )
    except (OSError, subprocess.SubprocessError):
        return None
    return result.stdout.strip() or None if result.returncode == 0 else None


@lru_cache(maxsize=1)
def built_at() -> str | None:
    """이미지를 만든 시각 (ISO8601). 없거나 모양이 아니면 None.

    모양을 확인하는 이유: 잘못된 값이 그대로 화면에 가면 `Invalid Date` 가 찍힌다.
    없는 것보다 나쁘다 — 사용자는 고장으로 읽는다.
    """
    raw = os.environ.get(BUILT_AT_ENV, "").strip()
    if not raw:
        return None
    try:
        dt.datetime.fromisoformat(raw.replace("Z", "+00:00"))
    except ValueError:
        return None
    return raw


@lru_cache(maxsize=1)
def version_string() -> str:
    """로그와 한 줄 표시용. 화면은 아래 `info()` 의 조각들을 직접 조합한다."""
    revision = git_revision()
    return f"{APP_VERSION} ({revision})" if revision else APP_VERSION


def info() -> dict:
    """화면에 내보낼 조각들. **합치지 않고 따로 준다.**

    합쳐서 보내면 화면이 그것을 다시 쪼개야 하고, 시각은 보는 사람의 시간대로
    찍혀야 하는데 서버는 그게 어디인지 모른다.
    """
    return {"version": APP_VERSION, "revision": git_revision(), "built_at": built_at()}
